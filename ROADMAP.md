# Vigil — Roadmap & Progress

Source of truth for *where we are*. The `release` gate proves a phase **passes**;
this file tracks what is **done** and **pending**. Update via the `progress` skill.

Legend: [x] done · [~] in progress / pending review · [ ] not started

---

## Phase 0 — Specs & Claude/agent setup  [x]
- [x] Seven specs filled, conformance green
- [x] Skills (data-cleaning, schema-migration, spec-conformance) + 4 subagents committed (5 with `eda`, added in Phase 1)
- [x] uv toolchain + CI green on GitHub
**Done when:** specs reviewed; `check_specs.py` green. ✅

## Phase 1 — Data: fetch, clean, synthesize  [x]
- [x] Pipeline runs end-to-end on REAL AACT: raw → clean → synthetic + quality + calibration reports (`make data`)
- [x] **Ran `--live` against a real AACT snapshot** (2026-06-05, hosted AACT): raw+manifest pinned under `data/raw/aact/2026-06-05/`; cleaned to ref_* (73,073 trials / 182,240 arms / 384,802 reasons), 760 fail-loud rows recorded
- [x] **EDA on real `ref_*` data** → `data/eda/` (overall dropout trial-mean 0.202 / participant-weighted 0.151; by phase/area/enrollment; reason mix; missingness; covariate↔dropout signs as expected)
  - viewable interactively: `make eda` → marimo notebook (`ingestion/eda/notebook.py`); generator is now `make eda-report`. Read-only `eda` subagent (`.claude/agents/eda.md`) answers data questions over the snapshot.
- [x] **SPEC RATIFIED to CTGOV2** (commit `857d505`): `specs/data.md` adopts CTGOV2 enum codes (+`ECT`); `schema.py`/`ctgov_enums.py`/`clean.py`/`vocab.py`/fixture reconciled. Quality report now 0 spec-contradictions / 0 unmapped-enums.
- [x] **Calibration targets populated from REAL EDA** (`targets.py`, provenance snapshot 2026-06-05): overall + per-stratum dropout (phase/area/sponsor_class/blinding/site-count), reason-mix (STUDY_TERMINATED/ADMINISTRATIVE incl., censoring excluded), enrollment+arm marginals, covariate-sign checks. `early_fraction` stays a labelled ASSUMPTION (AACT has no per-event timing — TODO `targets.py:250`).
- [x] **Synthetic cohort regenerated + calibrated** — deterministic (seed in manifest, bit-identical), `synthetic=True` on all 96,684 rows, SYNTHETIC README intact. **Calibration report 40/40 PASS** within spec tolerances (overall trial-mean 0.2016 vs 0.2018 ±1pp; all per-stratum ±3pp; reason-mix χ² p=0.58; timing assumption ±10pp; covariate signs matched; marginals matched), fails loud on any miss.
**Done when:** one command runs raw → clean → synthetic from REAL data, quality + calibration reports pass, validates against data spec. ✅
> DONE. Calibrated to the **trial-mean** overall (0.2018 — matches spec's "mean not_completed/started"); participant-weighted 0.151 recorded alongside (the two aggregations can't both be hit at the marginal level). Synthetic is generated from the real ref_* snapshot (deterministic stratified subsample ~8k trials → 96,684 participants), each trial at its own real rate. Engagement series capped 90d (new deterministic baseline). early_fraction remains a labelled assumption.

## Phase 2 — Skeleton: secrets, DB, auth, sessions, queue  [x]
- [x] Vault → Postgres+RLS → auth → scoped layer → Redis → Arq → seed → scoped user creation
- [x] Leakage test 7/7 (data isolation + user-creation subset), VERIFIED on real Postgres as NOBYPASSRLS `vigil_app`
- [x] Review pass (2026-06-05): RLS policies, JWT claim shape, scoped-creation match `domain.md`; job round-trip green
- [x] All 7 roles login + `/me` (platform admin, auditor, sponsor oversight, coordinator, PI, study manager, CRA) — `test_all_seven_roles_login_and_me`; CRA + PI now seeded
- [x] CI runs the fast suite (`pytest -m "not slow"`) so Phase-1 ingestion tests execute in CI, plus leakage + spine
**Done when:** login per role + both sponsors; leakage test passes (data + user creation); job round-trips. ✅
> Verified on real Postgres+Redis (full suite 45 passed, leakage 7/7).

## Phase 3 — Models  [x]
Baselines (logistic + GBT) on real registry · sequence model (synthetic cohort) · survival ·
calibration + SHAP · temporal-only eval (PR-AUC, recall@precision, lead-time gain) · scores → Postgres.
**Done when:** reproducible training from Phase 1 REAL data; metrics logged; scores behind RLS.
- [x] **Step (c) survival model** — discrete-time hazard on censored T2D synthetic cohort. C-index=0.3998 (concordance_index(tte, -H, event); see note below). `models/t2d/survival.py`; artifacts in `data/models/t2d/`; tests green. Censoring ~0.03% (non-informative admin cutoff, documented — not the clinical value driver).
- [x] **Phase 3 consolidated scorecard written** — `data/models/PHASE3_CARD.md` (2026-06-09): all numbers verified against source JSONs; negative survival result stated plainly; provenance ladder documented; central thesis + what-this-does-not-prove sections included.
> Phase 1 REAL data + EDA are DONE — Phase 3 is no longer blocked on them. Now blocked on two
> pre-conditions: (1) the **feature-contract spec** is not yet ratified into `specs/data.md`
> (scoped modelling cohort {PHASE1/PHASE2, PHASE2, PHASE2/PHASE3, PHASE3}; sponsor_class is a
> feature / identity is NOT; max_age_years missingness as boolean, not imputed) — per the
> per-phase ritual, spec on `main` first; (2) two open scoring decisions: (a) scoring-target
> mismatch — how `risk_score` reaches operational/demo participants with no engagement series;
> (b) baseline label definition (binary dropout-rate cut vs continuous vs overall_status/
> why_stopped — the latter not in the cleaned schema). Held-out split (group-disjoint by
> `nct_id`) is the Phase-3 test artifact (golden = transforms only).

## Phase 4 — Console, API & model routing  [~]
FastAPI per api spec · four screens (v0) via scoped layer · routing (regime, champion/challenger, drift-fallback, audited promotion).
- [~] Frontend v0 scaffolded (`frontend/`): four screens + login + docked assistant, shared `AppNav`/`AppChrome` (assistant gated off `/login`), fake auth context. **All data STUBBED** in `lib/stubs.ts`, typed to `specs/api.md` schemas (`lib/types.ts`). No backend, no real auth, no model routing.
- [ ] FastAPI endpoints per `specs/api.md`; wire each stub boundary to the scoped data layer (see frontend register below)
- [x] specs/routing.md ratified on main (routing spec-complete; BUILD pending — see line below)
- [ ] Model routing (regime, champion/challenger, drift-fallback, audited promotion)
  - [x] B1 built: routing_state migration + ORM, champion resolver wired in score_trial, t2d seed row
  - [x] **B2a-spec done**: engagement (visit trajectory) table spec ratified in `specs/scoring.md § Engagement (visit trajectory) input`; cross-referenced from `specs/data.md`; `check_specs.py` updated (11 required sections). Root cause for the block: the sequence LSTM was found unscoreable in B2 because no trajectory data existed in the operational DB — no engagement table, no static covariates on `participant`. The gate reframes subsequent work as:
  - [x] **B2a-spec-2 done** (corrected): covariate-provenance invariant (#10) ratified; imputed-capable participant covariates are exactly `age_years`, `hba1c_pct`, `bmi` (3 columns, each with `*_baseline_imputed` companion). Decision c: `planned_duration_days` is trial-level (read from trial record at feature-assembly time), NOT a participant column, carries no flag.
  - [x] **B2a-1 done**: engagement migration (0004) + RLS (FORCE, fail-closed, no platform bypass) + participant covariate columns (`age_years/hba1c_pct/bmi` + 3 `*_baseline_imputed` flags + `sex`) + `TENANT_TABLES` updated + `EXCLUDED_FROM_FEATURES` extended with 3 flags + invariants 6/7/10 tested. Invariants 8/9 deferred to B2b (require running scorer).
    - [x] **B2a-2 done**: seed bridge — synthetic T2D engagement seeded to demo participants (2026-06-10). Mapping: `synthetic_pid = uint32(MD5(uuid.bytes + b"seed-bridge-v1")[:4]) % n_participants` (deterministic, versioned, auditable). Reference epoch 2023-01-01 UTC. miss_probability withheld (inv 4). Covariates + *_baseline_imputed flags set from parquet provenance. 7 spine tests green. **B2b (LSTM artifact + _load_scorer wiring) is now unblocked.**
    - [x] **B2b done** (2026-06-11): real sequence LSTM now scores live from engagement — live path no longer random. `data/models/t2d/sequence_v1.0_demo.pt` persisted (test PR-AUC=0.3390, pre-registered bar 0.3006 PASSED). `LSTMScorer` wired as champion; `_demo_scorer` (rng) retained as explicit no-artifact fallback only. Trial context columns added (migration 0005: `trial.n_sites`, `trial.planned_duration_days`, `trial.phase`; `participant.arm_type`). Temporal guard + feature leakage guards wired in all paths. Invariants 8 (synthetic propagation) and 9 (single `_seq_feature_frame` builder) satisfied. 7 spine slow tests green (fidelity, scoreability, inv 8, inv 9, inv 7 live, artifact exists, LSTMScorer loaded). Invariant-7 live feature-time test isolated (order-independent); no longer green-by-collection-order. **Live escalation demo (inject worsening engagement → risk rises → climbs watchlist → alert) is NOW honestly claimable.** B2c (GBT shadow) and B3 (fallback/promotion) remain.
    - [x] **B2c done** (2026-06-11, **verified under real Postgres**): structural GBT (`HistGradientBoostingRegressor`) persisted as shadow alongside LSTM champion. `data/models/t2d/structural_v1.0_t2d.pkl` (test PR-AUC=0.3425, reload delta=0.0). Shadow registered in routing_state (role='shadow', model_version='structural_v1.0:t2d'). Shadow writes participant_score rows ONLY — never touches `participant.risk_score`/`risk_band` denorm cache. **Invariant (i) verdict (arbiter actually run vs Postgres, not code-read): shared-table design HELD.** Layer 1 (denorm cache, GET /cohort) surfaces champion only; layer 2 (GET /participants/{id}/risk) now surfaces champion only via the **champion-allowlist filter** `scoring.get_surfaceable_score(champion_versions=routing.champion_model_versions())` — the adversarial shadow row is filtered out, not merely RLS-hidden; layer 3 (RLS) shows BOTH rows to the sponsor (suppression is app-layer). Fail-closed proven: a shadow-only participant → `/risk` 404, never a non-champion fallback. Invariant (ii): shadow inference fires `assert_no_outcome_features` (no exemption). Torch isolation preserved (shadow is sklearn/joblib only; B2c tests carry no slow marker → run in CI Postgres). **8 spine tests** in `tests/spine/test_b2c_scoring.py` (added layer-2 fail-closed test), all green under Postgres. `StructuralScorer` + `_load_shadow_scorer` in tasks.py; `get_shadow` + `champion_model_versions` in routing repo; `get_surfaceable_score` in scoring repo; `risk_service.py` wires `/risk`; seed wires both champion + shadow rows. **routing.md § Storage open question marked RESOLVED (shared table); api.md `/risk` row notes champion-only + 404.**
  - [x] **Sacred-suite Postgres infra + skip finding** (2026-06-11): the spine/leakage suite gates on real Postgres (RLS is a DB feature); `tests/spine/conftest.py::migrated_db` SKIPS when no admin DSN is reachable. **Finding — CI HAS Postgres and HAS been running the sacred suite for real.** CI (`.github/workflows/ci.yml`) provides a `pgvector/pg16` service + Redis and runs `pytest tests/spine`; the non-slow Postgres-backed isolation tests — cross-tenant invariants 1–6 (`test_leakage.py`), scoring leakage (`test_scoring_leakage.py`), engagement leakage incl. feature-time + synthetic-flag guards (`test_engagement_leakage.py`), routing B1, dashboard role gates, seed bridge, worker — have been **green-for-real in CI**, not skipping. **BUT two gaps:** (1) the B2b **live** invariants 7/8/9 (`test_b2b_scoring.py`, `score_trial` end-to-end + real LSTM) are `slow`-marked and were excluded from CI by the default `-m "not slow"` addopts — they had **never run in CI**, only locally via `make test-slow`. The LSTM artifact is committed (27KB) and the slow spine suite runs in ~10s, so exclusion was by marker, not cost. **Fixed:** added CI step `pytest tests/spine -m "slow or not slow"` (full spine incl. slow live invariants, Postgres-backed; heavy 18k-cohort regeneration slow tests live outside `tests/spine` and are not pulled in). (2) B2c was uncommitted, so its invariant-(i) arbiter had run **nowhere** before this task (skipped locally for want of Postgres). Now run for real via local Docker Postgres (`docker compose -f docker-compose.dev.yml up postgres redis`) — full spine **78 passed, 0 skipped, 0 deselected**.
  - [x] **Feature-time leakage test failure root-caused + fixed** (2026-06-11): full-suite failure (`test_champion_untouched_after_shadow`, `test_scoreability`, `test_invariant_8` raising `LeakageError: future leakage`) root-caused as **diagnosis A (time-window) realised in the SEED** — the seed bridge's fixed `2023-01-01` epoch + `enrollment_day + planned_duration_days` projected longer trajectories' tail visits past wall-clock now(), so the operational DB held future-dated visits the feature-time guard correctly rejected. **Fixed at the SEED** (`vigil/seed.py::_seed_engagement`): anchor each trajectory so its last visit lands `_SEED_FUTURE_MARGIN_DAYS` before now(), preserving relative spacing/ordering (LSTM features unchanged). Guard (`assert_feature_time_before_t`) and scoring path UNTOUCHED; no pre-t filter added (a filter would mask invariant 7). Invariant 7 still fires on a genuine 2099 row. Full spine **78 passed, 0 skipped, 0 deselected**.
  - [x] **Fidelity test made hermetic (A)** (2026-06-11): `test_fidelity_reloaded_pr_auc` failed in CI (`FileNotFoundError` on gitignored `data/raw/aact/2026-06-05/…`) because it rebuilt the cohort split via `load_t2d_cohort()` — exposed for the first time by the new slow-spine CI step. Fixed HERMETICALLY: the `.pt` artifact now carries its frozen eval fold `test_nct_ids` (151 public NCT identifiers, no PHI; added in `scripts/persist_sequence_artifact.py`, artifact regenerated deterministically — `test_pr_auc` unchanged at 0.3390398896276865); the test scores that fold against the COMMITTED synthetic parquet and still asserts PR-AUC reproduction within 0.001 (invariant preserved). Proven hermetic by passing with `data/raw` moved aside. Fidelity now RUNS + PASSES in CI (not skipped). Sibling slow tests checked: only fidelity was raw-dependent (`test_scoreability`/inv-8/inv-9 use the seeded DB + committed synthetic; `test_seed_bridge` reads committed synthetic parquet) — none remain raw-dependent. Full spine **78 passed, 0 skipped, 0 deselected**.
  - [x] **B3 done — drift-triggered fallback + audited promotion** (2026-06-11): the last routing build gate. **Fallback** (automatic, system-initiated): `routing_service.handle_breach(BreachSignal(regime, model_version, breached))` consumes an OPAQUE breach signal (no drift computation/storage — that stays observability scope) and, on a breached current champion, rolls down to the last-known-good prior champion via a permitted `audit_log` history query (`routing_repo.last_known_good_champion`), updating `routing_state` + writing a `model_fallback` audit (actor=NULL, sponsor=NULL, `reason=drift_breach`, non-null `model_card_ref`); with no prior champion it sets `health='fallback'` and suspends — never auto-promotes the shadow. **Promotion** (manual, platform_admin only): `routing_service.promote(scope, …)` + `POST /monitoring/models/promote` (new `monitoring` router), 403 for non-platform_admin (incl. auditor), `actor_user_id` NEVER NULL, honesty hook enforced (`eval_provenance` required; synthetic→`architecture_validation`; clinical claim rejected; non-null `model_card_ref`). routing_state stays the current projection; `audit_log` immutable history, read only for rollback targets. Null-sponsor routing audit rows are platform-visible only (audit_scope). **Every transition writes exactly one audit row — nothing changes silently.** 7 spine tests in `tests/spine/test_routing_b3.py`. Spec touches: `api.md` (+`POST /monitoring/models/promote` row), `routing.md` (delivery-mechanism note: reaction-as-callable, source deferred to observability). check_specs stays 10. **Phase 4 routing done-when ("routing decisions logged, nothing changes silently") is MET.** Drift-signal SOURCE/delivery remains observability-phase scope (out of B3 by spec). Full spine **85 passed, 0 skipped, 0 deselected**.
  - [x] **B4 done — backend provenance debt closed before frontend wiring** (2026-06-11): consolidation of known soft spots so the UI sits on an honest backend. **Fix 1 — synthetic flag no longer faked:** `cohort_service.list_cohort` hardcoded `synthetic=True` on every CohortRow (a violation of api.md's "from participant_score.synthetic"); now data-driven from the CHAMPION `participant_score` row. **Fix 2 — top_factors data-driven:** was hardcoded `[]`; now from the champion row (empty stays empty, never fabricated). **Champion-only surfacing PRESERVED (B2c):** `risk_score`/`risk_band` still read the denorm cache (champion-only write guard); `synthetic`/`top_factors` read via the champion allowlist (`routing.champion_model_versions` → new batch `scoring.champion_scores_by_participant`), so a shadow/challenger row can NEVER surface — proven by a regression test with a divergent shadow row. No-champion-row participants get the safe-synthetic default (True — never falsely claims real) + empty factors. **Fix 3 — pan-indication base rate recorded (Finding 2 closed):** `data/models/baselines/metrics.json` test/val blocks now carry `positive_prevalence`/`n_positives`/`n_total` (test prevalence 0.4740 — PR-AUC 0.697 reads honestly above chance); recomputed hermetically from committed `data/clean/*.parquet` (threshold + n_arms matched the artifact exactly); `models/baselines.py` records it on future regen. 5 spine tests in `tests/spine/test_b4_cohort_provenance.py`. No spec change (code brought INTO conformance with api.md). check_specs stays 10. **Frontend wiring onto this honest backend is the next Phase-4 work.** Full spine **90 passed, 0 skipped, 0 deselected**; fast suite 156 passed.
  - [x] **H1 done — scoring appends timestamped history** (2026-06-11): `participant_score` changed from upsert-over-current to APPEND. Spec-first: `specs/scoring.md § Writeback` rewritten (one row per participant/version/run, identity = surrogate `id`, ordered by `computed_at`; `UNIQUE(participant_id, model_version)` REPLACED by composite index `(participant_id, model_version, computed_at)`), new § "Champion-only **+ latest** surfacing", append-idempotency + audit `n_rows` semantics; `routing.md § (i)` extended to "newest champion row". Migration `0006_participant_score_history` drops `uq_participant_score` + adds the index (RLS UNCHANGED — FORCE/fail-closed sponsor policy intact); applies on fresh DB + the 0001–0005 chain. `scoring_repo.upsert_score` → `append_score` (plain INSERT, no overwrite); `get_score` now newest-safe; `get_surfaceable_score`/`champion_scores_by_participant` already order `computed_at DESC` so champion-only surfacing = **newest champion** by construction. Callers renamed (seed, tasks ×2, 4 tests). 5 spine tests in `tests/spine/test_h1_score_history.py` (append-not-overwrite, newest surfaces via repo + HTTP `/risk`, **champion-only preserved with history + shadow**, history rows tenant-isolated, re-run appends). **B2c champion-only (i)/(ii) + B4 provenance tests STILL PASS** (verified: 29 passed across H1+B2c+B4+scoring_leakage). check_specs stays 10 (extends Writeback, no new heading). This is the data foundation for the risk-history endpoint (H2) and the sparkline (Wire-3). Full spine **101 passed, 0 skipped, 0 deselected**.
  - [x] **H2 done — risk-history read endpoint** (2026-06-11): `GET /participants/{id}/risk/history` returns a participant's CHAMPION risk **trajectory** over time (reads H1 appended history), ordered by `computed_at` ASC. Spec-first: `api.md § participants` row + `RiskHistoryPoint`/`RiskHistory` models added (no new `##` heading; check_specs stays 10). Champion-only via the B2c/B4 allowlist (`routing.champion_model_versions` → new `scoring.champion_history_for_participant`) — shadow/challenger points NEVER appear; same `require_scope`/RLS path as `/risk`; 403 for platform roles; **404 fail-closed** when no champion history (incl. out-of-scope, which RLS renders as no rows). Each point carries its real `synthetic` flag (B4 provenance, per-point, never smoothed). **Across-promotion semantic = (a) current champion version only** (clean single-version trajectory; pre-promotion points drop out) — chosen because it reuses the proven champion-allowlist guard (correct-by-construction, no promotion-history join) — **FLAGGED for ratification** (alt (b) = champion-at-each-point spanning versions). `risk_service.get_participant_risk_history` + `RiskHistoryPointView`. 6 spine tests in `tests/spine/test_h2_risk_history.py` (trajectory time-order, champion-only with shadow+challenger present, cross-tenant 404, per-point provenance both directions, empty→404, platform 403). Read-only — no schema/writeback change. **Unblocks the Wire-3 sparkline.** Full spine **107 passed, 0 skipped, 0 deselected**.
  - [x] **H2b done — risk-history is promotion-aware (semantic (b))** (2026-06-11): switched `GET /participants/{id}/risk/history` from H2's semantic (a) (current-champion-version only — silently truncated pre-promotion history) to **(b) champion-at-each-point**: each point is the row whose `model_version` was the CHAMPION-OF-RECORD at its `computed_at`, spanning version changes, each point honestly labeled (`model_version` + `model_card_ref` + `synthetic`) — a cross-version trajectory is visible, never smoothed. **Champion-at-each-point guard** (`routing.champion_version_intervals`) reconstructs the champion timeline per regime from B3 `audit_log` transitions (`model_promote`/`model_fallback` ordered by `created_at`) + current `routing_state` — a shadow/challenger row written DURING a champion's tenure is STILL excluded (only champion-of-record at its own timestamp qualifies); a version that was never champion never appears. `scoring.champion_history_for_participant` → `history_rows_for_participant` (raw rows, caller applies the timeline filter); `risk_service.get_participant_risk_history` rewritten + `_in_intervals`. **404→empty-200 fix:** in-scope participant with no champion points → `200` `points: []` (data state); out-of-scope/not-found (RLS-hidden) → `404`, platform role → `403` (security) — distinguished by a scoped participant-visibility check. api.md updated (semantic (b), empty-200, per-point `model_card_ref`); no new `##` heading (check_specs 10). 4 H2b tests (`test_h2b_history_promotion.py`: spans-promotion 4 pts X,X,Y,Y; non-champion-during-tenure excluded; no-promotion unchanged; out-of-scope 404) + H2 empty test updated to 200-empty. Read-only. Unblocks Wire-3 sparkline (now a real, FULL, honestly-labeled trajectory). Full spine **111 passed, 0 skipped, 0 deselected**.
  - [ ] **B2 dependency — regime not threaded**: `ScoringTriggerIn` has no `regime` field; `trigger_scoring` drops it; resolver hard-raise is unreachable from real API callers until wired through the trigger path
  - [ ] **B2 dependency — no indication column on `trial`**: operational `trial` table has no `therapeutic_area`/`regime` column; regime must derive from a persisted source (`ref_trial.therapeutic_area` exists in AACT ingestion but was never written to the operational `trial` table — requires migration + backfill decision)
  - [ ] **B2 seed — ALZ champion**: add a second regime (`alz`) seeded with the structural-only champion (real decomposition, PR-AUC 0.77, NOT a trajectory/synthetic model) to prove multi-regime dispatch end-to-end
- [x] scoring writeback: `participant_score` behind RLS (no platform bypass), Alembic migration 0002, `ParticipantScore` ORM in `TENANT_TABLES`, `POST /scoring/trigger` (202), `GET /scoring/jobs/{job_id}`, `POST /scoring/inject_events` (DEMO_MODE gated), Arq `score_trial` worker with jitter + `assert_no_outcome_features` + `run_smoke` guards, seed writes demo score rows, 5 isolation invariants in `tests/spine/test_scoring_leakage.py`
- [~] Dashboard data layer wired: real API client (lib/api.ts) replacing stubs; synthetic badge on watchlist + banner on participant panel; role-gate for ml_admin/auditor; demo loop (inject→poll→refresh→alert); participants router stub with correct 403 guards; synthetic added to CohortRow (api.md + cohort router + types.ts). STOP — user reviews before commit.
**Done when:** coordinator sees only scoped cohort; routing decisions audited.
> Frontend is stubs-only (typed to api.md). Stays [~] until the API exists and the boundaries below are wired through the scoped layer.

## Phase 5 — Agentic layer & RAG (local)  [x]
Retention/Report/Operations agents via scope-resolved tools in caller scope · hybrid RAG (structured + pgvector) ·
hand-rolled LLM-classify router + shared grounding spine (NOT LangGraph/MCP) + Langfuse · queued with caps + jittered retries + cost tracking · guardrails + PII redaction.
**Done when:** in-scope answered with citations; clinical/out-of-tenant refused. — ✅ **MET (Gate 5.5)**.
  - [x] **Gate 5.7 — RAG eval set + check_specs enforcement (RELEASE GATE) (2026-06-16) → PHASE 5 COMPLETE**: built the versioned, labelled local-assistant RAG eval set (`tests/eval/local_assistant_eval.json`, v2026-06-16) per `rag.md § Evaluation set` / `data.md` (eval set = RAG): 7 cases covering **faithfulness, citation precision/recall, answerable-vs-unanswerable, scope-faithfulness, and metric-grounding**. Runner `tests/spine/test_rag_eval.py` scores it against the **REAL sentence-transformers embedder** (semantic retrieval, offline/vendored — the only embedder) with a **stubbed LLM** for reproducible generation; **release gate = every case passes**. **Deterministic faithfulness** (stated in the eval file's `scoring` block): an answered turn must carry ≥1 grounding citation AND a turn with no retrievable grounding must REFUSE — the system never emits an ungrounded answer (LLM-judged sentence-level entailment needs a judge model / non-deterministic → deferred; this proxy verifies grounding presence, the boundary-level faithfulness contract). Citation precision = citations reference real retrieved sources (built from retrieval, not LLM text); recall = expected source (card `.md` for metrics) present; scope-faithfulness = no structured citation for an out-of-scope (cross-tenant/cross-site) participant; metric-grounding = a performance number is answered only with a card citation. Unanswerable case exercises the real grounded-refusal guarantee (clear grounding → refuse → restore). **`scripts/check_specs.py` extended** (the long-standing housekeeping TODO): when `rag.md` exists, the eval-set artifact MUST exist + carry cases covering all required dimensions — verified it FAILS when the set is absent, PASSes when present; **stays PASS (10 specs)**. NOTE/deferred: `rag.md § Grounding` says "retrieves nothing **relevant** → refuse" but the agent currently refuses on nothing **retrieved** (zero chunks); a cosine-distance relevance threshold (relevance-based refusal on a populated corpus) is a follow-up — not changed here to avoid a risky behaviour shift mid-run; the eval's unanswerable case therefore exercises the zero-grounding path. Full spine **150 passed, 0 skipped**; fast 99; golden 5; check_specs 10. **Phase 5 (agentic layer & RAG) COMPLETE.**
  - [x] **Gate 5.6 — Report + Operations agents on the SAME spine (2026-06-16)**: the router now dispatches to all THREE agents (Retention/Report/Operations) by classified intent; unclassifiable/unsafe/out-of-scope still REFUSED at the router. Extracted the shared grounding/citation pipeline into `vigil/agents/agent_base.py` (`grounded_answer` + `AgentAnswer`) — ONE spine: 5.4 `agent_tool_context` (full-Scope re-resolution, `scoped_session` ONLY, never `sponsor_bootstrap_session`) → champion-only `participant_risk_facts` + RLS `search_documents` → doc-grounded, cited answer → grounded refusal when nothing retrieved. `retention.py` refactored onto it (behaviour identical — 5.5 turn tests still green); new `report.py` (scoped reporting/aggregates) + `operations.py` (scoped operational/status; refuses promotion/fallback ACTIONS — those stay the audited platform_admin path) differ ONLY by system prompt. `tasks.run_assistant_turn` dispatches via `_AGENTS = {retention, report, operations}` (unknown id → fail-closed refusal); per-turn `message_events` tagged `agent:<name>`. **SACRED dual-axis isolation re-proven END-TO-END for ALL THREE agents** (`tests/spine/test_report_operations_agents.py`, parametrized report+operations): cross-tenant (Sponsor B facts never surface), cross-site-within-tenant (site-scoped coord can't reach another site — fresh trial/site fixture, no seed pollution), champion-only (shadow never in citations), in-scope-answered-with-citations + correct event tag. LLM stubbed (`_llm_override`); no live calls. Full spine **148 passed, 0 skipped**; fast 99; check_specs 10; golden 5. 5.7 (eval set + check_specs enforcement) remains.
  - [x] **Gate 5.5b — Anthropic primary LLM + OpenRouter fallback (2026-06-16)**: multi-provider resilience behind the existing `get_llm_client()` factory; agents untouched. **Anthropic `claude-haiku-4-5` is PRIMARY**, OpenRouter is FALLBACK. New `AnthropicClient` (lazy `import anthropic` inside `complete` — stays out of stub/golden/check_specs import paths, verified; maps SYSTEM messages → the Messages API top-level `system=` param, user/assistant → `messages`; reads `usage.input/output_tokens`). New `FailoverClient` (ordered [Anthropic, OpenRouter]): fails over ONLY on **transient** errors (429 / 5xx / timeout / transport, `LLMError.retryable=True`); **re-raises on 401/400** (auth/bad-request) so a primary misconfig surfaces loudly, never masked. `LLMError` enriched with `status_code` + `retryable`; `OpenRouterClient` errors now classified symmetrically. `LLMResponse.model` records who actually answered. `get_llm_client()`: stub check **FIRST** (covers BOTH providers in CI — zero live calls, no keys), then Anthropic-primary (fail-loud if primary enabled but key missing — no silent downgrade) + OpenRouter fallback → `FailoverClient` (or single client). Config: `anthropic_enabled`(true)/`anthropic_model`(`claude-haiku-4-5`)/`anthropic_base_url`/`anthropic_cost_per_1k_tokens` + `anthropic_api_key` cached_property; secret `ANTHROPIC_API_KEY="vigil/llm/anthropic_api_key"` + `VIGIL_ANTHROPIC_API_KEY` shim; `llm_*` kept as OpenRouter/fallback (no rename). `anthropic` SDK added. **Spec-first:** `rag.md § Decisions` updated (Anthropic primary + OpenRouter fallback on transient; egress allow-list widened to `api.anthropic.com` + `openrouter.ai`); check_specs stays 10. 10 unit tests (`test_llm_failover.py`: retryability classify, failover-on-transient, no-failover-on-auth, all-transient-raises-last, Anthropic system-split, empty-key fail-loud, factory stub-first/failover/openrouter-only/primary-missing-fail-loud). Full spine **140 passed, 0 skipped**; fast 99; golden 5 (no anthropic/httpx in import path); check_specs 10. CI stays stubbed for both providers.
  - [x] **Gate 5.5 — router + Retention agent wired end-to-end → PHASE 5 DONE-WHEN MET (2026-06-15)**: a real turn flows `POST /assistant/conversations/{id}/messages` (202, enqueues `run_assistant_turn`, never inline) → **redact+guardrail** (5.2) → **router** (LLM-classify intent / refuse-at-router, `vigil/agents/router.py` — NOT a trained model; fail-closed JSON parse → refuse) → **Retention agent** (`vigil/agents/retention.py`: 5.4 `agent_tool_context` → `participant_risk_facts` champion-only + `search_documents` RLS-scoped → **grounded + cited** answer; metric numbers grounded in the card corpus, never inlined; zero retrieval → grounded refusal) → **one redacted `message_events` row** (5.1, every outcome). `GET /assistant/jobs/{id}` polls; `GET …/messages` = redacted transcript. New `assistant` router (registered in app.py) + `assistant_service` (open/post/get/list) + `run_assistant_turn` Arq task (registered in WorkerSettings). Turn **re-resolves full scope from user_id** (5.4) — dual-axis isolated through the full turn. **CI HERMETIC/STUBBED:** `VIGIL_LLM_STUB=true` (ci.yml env + spine conftest) → `StubLLMClient`, **no live OpenRouter call, no provider key needed**; tests inject a scripted stub. 9 spine tests (`test_assistant_turn.py`): done-when happy path (cited answer + event), guardrail-clinical block, router-refuse, cross-tenant + cross-site no-facts-through-turn, doc-grounded metric citation, champion-not-shadow, grounded refusal, endpoint-202. Full spine **140 passed, 0 skipped**; fast 89; check_specs PASS (10); golden 5. Remaining: **5.6** (Report + Operations agents) + **5.7** (RAG eval set + check_specs enforcement).
  - [x] **Gate 5.4 — scoped MCP tool layer + queued-agent scope propagation (KEYSTONE) (2026-06-15)**: the safe tools an agent uses + the scope-bound context they run in. No router/LLM/reasoning (5.5). `vigil/agents/tools.py`: **`agent_tool_context(user_id, requested_sponsor=None)`** = the chokepoint — **re-resolves the FULL `Scope` from `user_id`** via `resolve_scope` at job time (freshest grants; NOT a serialized scope/bare sponsor_id), then opens **`scoped_session(scope)`** and yields a `ToolContext` (scope + scoped session + champion allowlist). **NEVER `sponsor_bootstrap_session`, NEVER a raw factory** (asserted by an AST/namespace no-bypass test). Tools: `participant_risk_facts` (champion-only via `get_surfaceable_score`/`champion_model_versions` — never raw participant_score/shadow) and `search_documents` (5.3 `search_chunks`, RLS-scoped). **DUAL-AXIS isolation enforced + proven:** axis-1 cross-tenant = sponsor RLS; **axis-2 cross-site-within-tenant** = RLS is sponsor-only, so the structured tool ADDITIONALLY checks `scope.permits(ScopeTuple(sponsor,trial,site))` — a site-scoped coordinator reaches ONLY their site (the leak a sponsor-only opener would produce). Out-of-scope id → empty, never an error-leak. 7 spine tests (`test_agent_tools_isolation.py`): **cross-site coordinator-cannot-reach-other-site** (the landmine), sponsor-role-reaches-all-sites, cross-tenant blocked + out-of-scope empty, champion-not-shadow via tool, vector cross-tenant isolation, no-bootstrap/no-raw-factory, scope-re-resolved-from-user_id. Self-contained test fixtures (new trial/sites/coordinator) — no seed-count pollution. Full spine **131 passed, 0 skipped**; fast 89; check_specs PASS (10); golden 5 (ST never loaded). Next: **5.5 — router + Retention agent on `/assistant`**.
  - [x] **Gate 5.3 — pgvector + local embeddings + document_chunk corpus (2026-06-15)**: the hybrid-RAG vector substrate, no LLM/agent/router. Migration `0008_document_chunk` (down_revision `0007`): `CREATE EXTENSION vector` + `document_chunk` (id, sponsor_id NULLABLE, source_ref, chunk_index, chunk_text, `embedding vector(384)`, created_at) + hnsw cosine index + btree indexes. **CORPUS-SCOPE DECISION (a) — GLOBAL-OR-SPONSOR, RLS-ready (FLAGGED for ratification):** `sponsor_id` NULL = a GLOBAL project doc (model cards + PHASE3_CARD = the Phase-5 doc-grounding corpus, readable by all); non-null = sponsor-scoped (DEFERRED protocol/SOP docs), fail-closed like participant_score. One policy `document_chunk_scope` FORCE RLS — READ `USING (sponsor_id IS NULL OR is_platform OR sponsor_id = current_sponsor)`, WRITE `WITH CHECK (is_platform OR sponsor_id = current_sponsor)` (only platform writes globals; a sponsor writes only its own, never global/cross-tenant) → adding sponsor docs later needs NO RLS re-architecture. **LOCAL embeddings** (`vigil/agents/embeddings.py`): scoped retrieval `repositories/documents.py` (`add_chunk`, `search_chunks` cosine top-k under RLS — never hand-rolls the sponsor filter). Corpus ingest `vigil/agents/corpus.py` + `scripts/ingest_card_corpus.py` (globals under platform_session, idempotent per source). `pgvector` added to deps. **Embedder decision (B), 5.3-fix:** the SINGLE real embedder everywhere (CI/tests/demo) is **sentence-transformers all-MiniLM-L6-v2 (dim 384)**, **VENDORED in-repo** at `data/models/embeddings/all-MiniLM-L6-v2/` (~88 MB, tracked like the `.pt`/`.pkl` artifacts) and loaded **OFFLINE by construction** — the embedder forces `HF_HUB_OFFLINE`/`TRANSFORMERS_OFFLINE` and loads from the local dir, so NEITHER load NOR encode reaches the network (proven by a sockets-blocked load+embed test). **HashingEmbedder DROPPED** — no lexical fallback, no env-var backend switch, impossible to silently default to non-semantic. `sentence-transformers` added as a hard dep; `scripts/vendor_embedding_model.py` re-vendors. Determinism: CPU eval-mode, L2-normalised, run-to-run stable (asserted `atol=1e-6`). `sentence_transformers`/torch lazy-imported → golden/check_specs paths never load them (golden stays ~0.1s). CI unchanged: weights in-repo, offline forced in code (no per-run download). Tests: 4 fast (`test_embeddings.py`: vendored-present, **real-ST-offline-no-network**, deterministic, semantic-over-lexical) + 4 spine (`test_document_retrieval.py`: cross-tenant sponsor-chunk isolation + globals shared, citable card-corpus retrieval, **semantic ranking**, extension). Full spine **124 passed, 0 skipped**; fast 89; check_specs PASS (10). Next: **5.4 — scoped MCP tool layer + scope-propagation keystone** (⚠ adversarial cross-tenant + cross-site sacred test).
  - [x] **Gate 5.2 — LLM client (OpenRouter) + PII-redaction boundary + guardrail wrapper (2026-06-15)**: the safety boundary every turn passes through, BEFORE any router/agent/retrieval. New `vigil/agents/` package (the in-app agent layer; isolation.md walls the Guide from importing it). **LLM client** (`agents/llm.py`): `LLMClient` protocol + `OpenRouterClient` (httpx deferred-import; targets ONLY the configured OpenRouter base URL = egress allow-list; fail-loud `LLMError`) + deterministic hermetic `StubLLMClient` (records calls); `get_llm_client()` returns the stub when `VIGIL_LLM_STUB=true` (CI) else OpenRouter (key from Vault/env via settings, never inlined). Config: `llm_base_url`/`llm_model` (free model)/`llm_stub`/`llm_timeout_seconds`/`llm_max_tokens`/`llm_cost_per_1k_tokens`; `httpx` promoted to main deps (uv.lock synced). **Redaction** (`agents/redaction.py`): `redact()` strips email/phone/SSN/MRN/DOB/address/long-digit ids, **fail-loud** `RedactionError` (turn blocked, raw never returned/persisted); coded-id contract keeps free-text names out structurally. **Guardrails** (`agents/guardrails.py`): `check_content` blocks clinical/diagnostic + prompt-injection + secret-extraction (allowed otherwise); `guard_inbound` = redact-FIRST then content-check → `InboundGuard(redacted_text, GuardrailResult)`; `fence_untrusted` marks retrieved/tool text as DATA-not-instructions. Decision is produced here; the caller persists via 5.1 `write_message_event`. Tests: 12 fast unit (`tests/test_agents_boundary.py`) + 2 spine (`tests/spine/test_guardrail_turn.py`: blocked turn → `guardrail_decision=blocked`/`refused`; allowed turn → raw PII never in the stored redacted row). Full spine **120 passed, 0 skipped**; fast suite 85 passed; check_specs PASS (10). NO router/agents/retrieval (5.4/5.5).
  - [x] **Gate 5.1 — message_events table + redacted-write path (2026-06-15)**: schema + ORM + RLS + typed write, no LLM/agent. `MessageEvent` ORM ([db/models.py]) + migration `0007_message_events` (down_revision `0006`) to the observability.md field list (conversation_id, request_id, sponsor_id NULLABLE, role_or_guest_scope, surface, route_or_agent, guardrail_decision, retrieved_chunks jsonb, llm_provider_model, latency_ms, token_cost_estimate numeric, status, redacted_user_msg, redacted_assistant_msg, ts; indexes on (conversation_id,ts) + (sponsor_id,ts)). **Cross-tenant-BY-ROLE RLS like `audit_log`** (NOT the default sponsor predicate; NOT in TENANT_TABLES): `message_events_scope` = `USING (is_platform OR sponsor_id = current_sponsor)` + FORCE RLS → sponsor sees own, platform/auditor see all, null-sponsor (Guide-class) rows platform-visible only. Typed redacted-write repo `observability.write_message_event` (mirrors `write_score_audit`/`write_routing_audit`) — **content arrives ALREADY REDACTED** (redaction LOGIC is 5.2); no raw-content column/param, so raw text cannot be persisted by construction. `MessageEventOut` (api.md) read shape aligns (Phase-6 endpoint not built — shape confirmed, no drift). 3 spine tests (`test_message_events.py`: cross-tenant-by-role incl. null-sponsor-platform-only, write round-trip+schema, api-read-shape/no-raw-attr). Full spine **118 passed, 0 skipped**. check_specs PASS (10). admin/inspect surface + Langfuse = Phase 6.
  - [x] **Gate 5.0 — rag.md spec reconciled (2026-06-15)**: SPEC ONLY, no code. Added the Phase-5 contracts to `specs/rag.md`: **three agents** (Retention/Report/Operations, each scope-bound, tools + refusals defined); **router** (LLM-classification dispatch, explicitly NOT a trained model, refuses-at-router for unclear/unsafe/clinical/out-of-scope/no-match); **champion-allowlist binding** (agents read risk facts ONLY through the committed `get_surfaceable_score`/`/risk`·`/risk/history`·`/cohort`·detail services — never raw `participant_score`, shadow/challenger can never surface); **doc-grounding** (any metric/PR-AUC/lift/`n` retrieved from the model-card corpus `data/models/**/*model_card*.md` + `PHASE3_CARD.md` and cited, NEVER inlined — hardening updates cards, RAG inherits truth); **scope propagation** (agent Arq jobs re-resolve the FULL `Scope` via `resolve_scope` at job time and run every tool through `scoped_session` — NEVER `sponsor_bootstrap_session`/raw factory, which would drop site/trial tuple-narrowing → cross-SITE leak; sacred extension adversarial on BOTH cross-tenant AND cross-site-within-tenant); **retrieval stack** (LOCAL embeddings over pgvector `sponsor_id`-filtered; Phase-5 corpus = model cards + PHASE3_CARD only, protocol/SOP collections DEFERRED); **LLM/CI posture** (generation via OpenRouter free model, egress allow-listed to OpenRouter only; CI hermetic with recorded/fake router+agent responses); **eval set** named (faithfulness, citation P/R, answerable-vs-unanswerable refusal, scope-faithfulness, metric-grounding) as a CI release gate. **`message_events` WRITE path confirmed Phase 5** (a turn is un-auditable without it), admin/inspect surface stays Phase 6 — corrects roadmap-prose misplacement. **Done-when = router + Retention agent end-to-end** (Report/Operations follow-on). `check_specs.py` extended with the 6 new rag.md `##` headings (existing headings byte-identical) — stays **PASS (10 specs)**. api.md/observability.md needed NO change (assistant endpoints + message_events schema already specced). Proposed build-gate order: **5.1** message_events write path → **5.2** LLM client + PII redaction + guardrails → **5.3** pgvector + local-embedding doc corpus → **5.4** scoped MCP tool layer + scope-propagation keystone (⚠ adversarial cross-tenant + cross-site sacred test) → **5.5** router + Retention agent on `/assistant` → **5.6** Report + Operations agents → **5.7** RAG eval set + `check_specs` enforcement.

### Security follow-ups (cross-phase)
- [x] **SEC-1 — site-narrowing closed on cohort/participant READ APIs (2026-06-17, approach B / service-layer)**: the gap (Phase-6 recon): participant/intervention RLS keys on `sponsor_id` ONLY, but `domain.md` scopes site roles to their site (coordinator/PI = own site & trial; CRA = assigned sites) and `:53` says RLS admits only in-scope `(sponsor,trial,site)` rows. The agent tools (5.4) narrowed via `scope.permits`; the **direct read APIs did not** → a site coordinator/CRA saw the WHOLE sponsor's cohort/participants. **Approach (A) RLS-level REJECTED:** the caller's scope is a SET of tuple-COUPLED `(sponsor,trial,site)` entries (a CRA can hold site-1/trial-A + site-2/trial-B); a flat RLS GUC predicate (`site_id IN … AND trial_id IN …`) can't express the coupling (admits cross-products), per-tuple matching needs unsafe delimited-list GUC parsing, and `scoped_session` binds one sponsor while the scope carries many `(trial,site)` under it. **Approach (B) service-layer chosen** — new `vigil/services/scope_filter.py::participant_visible(scope, sponsor_id, trial_id, site_id)` = `scope.permits(ScopeTuple(...))`, the SAME tuple-coupled guarantee the 5.4 agent tools use; RLS stays the cross-TENANT (sponsor) guarantee unchanged (FORCE/fail-closed), the service layer adds cross-SITE narrowing. Applied to **every site-scoped read**: `cohort_service.list_cohort` (filter rows), `participant_service.get_detail`/`list_interventions`/`log_intervention`, `risk_service.get_participant_risk`/`get_participant_risk_history` → out-of-site → 404/empty/None. Agent-tool behaviour unchanged. Sponsor-oversight (`(sponsor,*,*)`) NOT over-narrowed (permits all sponsor sites); platform reaches no participant rows via RLS anyway. 4 spine tests (`tests/spine/test_sec1_site_narrowing.py`, HTTP-level): coordinator `/cohort` excludes other-site; CRA only assigned site; sponsor-oversight sees all sites; cross-site `/participants/{id}` + `/risk` + `/risk/history` + interventions (GET+POST) all 404 while own-site stays 200. NOTE: app-layer enforcement (consistent with 5.4); an RLS-level encoding could revisit later but is blocked by the tuple-coupling/list-parsing constraints above. Full spine **154 passed, 0 skipped**; fast 99; check_specs 10; golden 5.

## Phase 6 — Observability (both surfaces)  [x]
`message_events` + structured logs · Langfuse · admin page (inspect/verify-guardrails/debug-retrieval/show-redaction) · wire monitoring + cost screens.
**Done when:** every message → event row; guardrails + retrieval inspectable; redaction verifiable.
> IN PROGRESS. `message_events` is **built (5.1) and written every assistant turn (5.5/5.6)** — the
> data exists. Remaining: the inspect/admin READ surface + page, cost/latency capture, the /drift
> read surface, Langfuse tracing, and wiring the deferred Phase-4 monitoring/cost screens.
  - [x] **Gate 6.4 — admin/observability page + wired monitoring/cost screens (Phase 6 DONE-WHEN) (2026-06-17)** — frontend-only; backend (6.1/6.2/6.3) untouched. NEW `app/observability/page.tsx` over **GET /monitoring/messages** delivers the four duties: inspect messages (filter by surface/guardrail_decision/status/role/conversation_id, limit 100), verify guardrails fired (decision badge + filter), debug retrieval (expandable `retrieved_chunks` citation refs per turn), show redaction (only `redacted_user_msg`/`redacted_assistant_msg` + coded ids — no raw exists, no identifiable participant data). **Platform/auditor gate** (`PLATFORM_ROLES`, like triage): non-platform users see "available only to platform and auditor roles" and the nav link is hidden — the 6.1 API 403 is the real guard (defence-in-depth, frontend is UX). Wired the deferred screens to real endpoints, removing the Phase-3 stubs + fabricated data: `app/monitoring` → real `getModels` (champion/shadow from routing_state) + `getDrift` rendering the **honest-empty** state ("no drift data computed yet", NOT a fabricated chart); `app/costs` rewritten to real `getCost` rollups (total turns/cost + per-surface/model table) with an explicit **honest-zero** note (real token/latency counts; $0.00 until a cost rate is configured) — dropped the fabricated spend chart / traffic split / budget alerts. `lib/api.ts` gains `getMessages`/`getCost` (+ `getModels`/`getDrift` now real); `lib/types.ts` `MessageEventOut` extended to the full 6.1 shape + `CostReport`/`CostRollup`/`Citation`; `lib/stubs.ts` monitoring stubs removed; nav gains role-gated **Observability** link. **Gates:** `tsc --noEmit` clean; full spine **167 passed** (backend unchanged); check_specs **10**. Note: `/monitoring/messages` `status` filter is sent as the `status_filter` query key (the 6.1 endpoint param name). **Phase 6 DONE-WHEN met:** every message → event row (5.1/5.5); guardrails + retrieval inspectable; redaction verifiable. Next: **Phase 7** (public Guide demo, isolated).
  - [x] **Gate 6.3 — Langfuse per-turn tracing (real instance, CI-hermetic) (2026-06-17)** — the richer per-turn trace ON TOP of the durable `message_events` record. `vigil/agents/tracing.py`: `TurnTrace` (redacted-only — NO raw field by construction, only the `redacted_*` text + structural metadata the event row carries), `Tracer` protocol, `NullTracer`, `LangfuseTracer` (lazy-imports the `langfuse` SDK; v4 `create_event` + `flush`), `get_tracer` (→ `NullTracer` unless `langfuse_enabled`, checked BEFORE any key read/SDK import; tracer-build failure falls back to `NullTracer`), `safe_trace` (swallows any failure). `run_assistant_turn` emits one trace per outcome **after** the mandatory `message_events` write, **best-effort** — a disabled/failing Langfuse never breaks the turn or the audit row. **CI-HERMETIC:** `VIGIL_LANGFUSE_ENABLED` (default **false**) OFF in `ci.yml` + spine conftest → NullTracer, NO Langfuse call, NO SDK import, NO key (mirrors `VIGIL_LLM_STUB`); real Langfuse only local/demo. **EGRESS:** the Langfuse host (`VIGIL_LANGFUSE_HOST`, default `https://cloud.langfuse.com`) added to the deny-by-default allow-list alongside `api.anthropic.com`/`openrouter.ai` (NetworkPolicy in infra.md). **VAULT KEYS (place later):** `secret/vigil/langfuse/public_key` + `secret/vigil/langfuse/secret_key` (KV v2, field `value`); local-dev env shim `VIGIL_LANGFUSE_PUBLIC_KEY`/`VIGIL_LANGFUSE_SECRET_KEY` — never in code/repo/.env. Dep `langfuse>=2.0.0` added (lazy-imported, kept off light/golden import paths). **Proof:** unit (`tests/test_tracing.py`) — TurnTrace has no raw field; disabled→NullTracer; enabled-build-failure→NullTracer fallback; safe_trace swallows; module import pulls no `langfuse` (subprocess). spine (`tests/spine/test_tracing_turn.py`) — disabled→NullTracer + turn writes its row; redacted-only (raw email never in the trace payload, `[REDACTED_EMAIL]` instead); failing tracer doesn't break the turn or the audit row. SPEC: observability.md § Langfuse + infra.md NetworkPolicies extended (no new heading) → check_specs stays 10. Next: **6.4** frontend admin/observability page + wire deferred monitoring/cost screens.
  - [x] **Gate 6.2b — router-classification cost attributed + routing-approach decision recorded (2026-06-17)** — closed the 6.2 undercount: the router's LLM-classification call now carries its own usage (`RouteDecision` gains `provider_model`/`latency_ms`/`token_cost_estimate`/tokens, captured from the classify `LLMResponse`), threaded into `run_assistant_turn`. The `message_events` row now reflects the **TURN TOTAL**: answered turn = router-classify **+** agent-generation (cost/latency summed; `llm_provider_model` = the answering agent's provider); refusal-at-router = the router's real (non-zero) classification cost alone (no longer honest-zero); a guardrail block **before** the router (no LLM call) stays genuinely **zero**. Cost derivation unchanged — real tokens × configured rate, honest-zero only when no LLM call was made; no fabrication. **Cost tracking is now COMPLETE (no undercount).** DECISION recorded (rag.md § Router → *Routing-approach decision (ratified)* + here): the router classifies intent via an **LLM call, NOT a trained classifier** — RATIONALE: no labeled routing dataset + timeline didn't justify building/training/eval/versioning a classifier; an LLM prompt is correct-by-construction against the agent definitions with zero training data. COST: every turn makes a router-classify call (now attributed). FUTURE PATH: a small **trained intent classifier** mined from `message_events` (question→agent) could replace the LLM router later — cheaper/faster, no per-turn LLM cost — a data-dependent future optimization, not a current gap. **Proof (`tests/spine/test_monitoring_endpoints.py`):** answered turn cost == router+agent (149ms / 0.00050); refusal-at-router == router only (12ms / 0.00005, non-zero); guardrail-pre-router block == zero. SPEC: `### Routing-approach decision` is a subsection under the existing `## Router` (no new required heading) → check_specs stays 10. Next: **6.3** Langfuse tracing.
  - [x] **Gate 6.2 — cost/latency capture + /cost·/models·/drift read surfaces (2026-06-17)** — closed the 0-cost capture gap and built the remaining monitoring read endpoints to the 6.0 contract. **Capture:** `AgentAnswer` now carries provider usage (`provider_model`/`latency_ms`/`token_cost_estimate`/tokens), threaded from the `LLMResponse` in `agent_base.grounded_answer` → `run_assistant_turn._write` → `message_events`, so a turn persists the **real** provider latency/cost (no more 0-default) and records WHICH provider answered; a grounded/router refusal makes no generation call so it stays **honest-zero** (never fabricated). **Cost derivation:** `cost_estimate = (prompt+completion tokens)/1000 × configured per-1k rate` (`{anthropic,llm}_cost_per_1k_tokens`); if no rate is configured the rate is 0 → honest-zero cost with the token counts still real — never a fabricated dollar figure. **Read endpoints (all PLATFORM/AUDITOR ONLY, 403 for sponsor/site — `_require_platform` reuses the 6.1 gate):** `GET /monitoring/cost` rolls up ONLY real stored `token_cost_estimate`/`latency_ms` by surface+model under `scoped_session` (RLS-bound, no cross-tenant aggregate leak); `GET /monitoring/models` reflects `routing_state` champion/shadow (global infra table, no RLS → platform_session after the gate); `GET /monitoring/drift` is **HONEST-EMPTY** (`items: []`) — drift is not computed/stored, **real drift computation is a DEFERRED TODO** (rolling PSI/AUC/prediction-drift writing rows), future work NOT Phase 6, marked `TODO(drift)` in `monitoring_service.list_drift_points`. SPEC: `api.md § monitoring` gains `CostQuery`/`CostRollup`/`CostReport` + DriftQuery/honest-empty notes (the endpoints' shapes); no new heading → check_specs stays 10. **Proof (`tests/spine/test_monitoring_endpoints.py`):** capture — a turn persists latency 137ms / cost 0.00045 / provider `anthropic/claude-haiku-4-5`; refusal — honest-zero; /cost — rollup sums real seeded usage, 403 for coordinator/oversight; /models — champion `sequence_v1.0:demo` + shadow present, 403 for coordinator/CRA; /drift — items==[], 403 for coordinator/PI. Next: **6.3** Langfuse tracing (egress allow-list, CI-hermetic).
  - [x] **Gate 6.1 — GET /monitoring/messages inspect API (2026-06-17)** — the heaviest/sacred Phase-6 read surface, built to the 6.0 contract. `monitoring_service.list_message_events` + `observability.query_message_events` + the router endpoint (`MessageEventOut` shape per api.md, extended to carry the redacted text + `retrieved_chunks` + route/cost so the page's four duties are servable). **PLATFORM/AUDITOR ONLY** — role gate (`scope.role not in PLATFORM_ROLES → MonitoringPermissionError → 403`) is the primary guard; **RLS-bound** — the query runs under `scoped_session(scope)` (NEVER `platform_session` unconditionally), so cross-tenant-by-role visibility comes from the `message_events_scope` policy (`is_platform OR sponsor_id=guc`), the role gate + RLS both present (defence in depth); **redacted-only** — no raw column exists, only `redacted_*` fields surface. Filters: surface/conversation_id/role/guardrail_decision/status/since/until + limit, ordered ts desc. **Adversarial proof (`tests/spine/test_monitoring_messages.py`):** role gate — admin+auditor 200, coordinator/PI/CRA/sponsor-oversight/study-manager all **403**; cross-tenant-by-role — auditor/platform see A + B + Guide events; **RLS backstop** (repo-level) — a Sponsor-B-bound session sees only B's events, never A's; redacted-only (no `user_msg`/`assistant_msg` keys) + guardrail_decision=blocked filter. SPEC: api.md `MessageEventOut` extended (redacted text + retrieved_chunks + route/surface/cost) to fulfil observability.md's admin-page duties; check_specs stays 10 (no heading change). Full spine **158 passed, 0 skipped**; fast 99; golden 5; check_specs 10. Next: **6.2** cost/latency capture + /cost·/models·/drift read surfaces.
  - [x] **Gate 6.0 — Phase 6 spec reconciled (2026-06-17)**: SPEC ONLY, no code. `observability.md` gains **§ Phase 6 contracts**: (1) **inspect endpoint scope contract** — `GET /monitoring/messages` is PLATFORM/AUDITOR ONLY (403 for sponsor/site), runs under `scoped_session` (RLS-bound, never `platform_session` unconditionally → never widens the cross-tenant-by-role boundary), redacted fields only (no raw column); (2) **cost/latency capture required** — the turn must thread `LLMResponse` usage into `message_events.latency_ms`/`token_cost_estimate` (currently default 0); cost reported real or honest-zero, **never fabricated**; (3) **/drift read-surface only** — drift not computed/stored (B3 deferred the source); Phase 6 builds the read surface returning **honest-empty**, real drift computation an explicit DEFERRED TODO (future, not Phase 6); (4) **Langfuse** — real instance holds full per-turn trace, table is durable record, traces carry only redacted content, new outbound egress (allow-listed, deny-by-default), CI hermetic (disabled/stubbed). `api.md § monitoring` prose updated to match (all `/monitoring/*` platform/auditor-only; inspect RLS-bound/redacted; drift honest-empty; cost real-or-absent). Corrected the **stale ROADMAP line** ("message_events has no implementation / never been run" — false since 5.1/5.5). `check_specs.py` registers the new `## Phase 6 contracts` heading (existing headings byte-identical) — **stays PASS (10)**. No code/logic touched. Gate order: **6.1** inspect API (⚠ sacred: platform/auditor-only + RLS-bound, doesn't widen) → **6.2** cost/latency capture + `/monitoring/{cost,models,drift}` read surfaces → **6.3** Langfuse tracing (egress allow-list, CI-hermetic) → **6.4** frontend admin/observability page + wire deferred monitoring/cost screens.

## Phase 7 — Public Guide demo (isolated)  [x]
Separate service, own creds · landing/demo site · Guide RAG over approved docs only · guardrails · emits message_events.
**Done when:** answers from approved docs; refuses out-of-scope; proven unable to reach real resources even when prompted.
> OPENED (Gate 7.0). The Guide is THE isolation phase — a separate public service that must be PROVABLY
> unable to reach any real-system resource. **Gate order:** 7.1 skeleton (separate, own creds, own
> message_events sink) → 7.2 RAG over approved docs + the Guide's own file-backed index (incl.
> relevance-threshold refusal) → 7.3 guardrails + redaction (owned copy) → **7.4 isolation-proof suite
> (SACRED — static import-graph/config-audit/tool-surface + behavioral red-team/egress-zero; the 5.4
> analog)** → 7.5 landing/demo site. Layer-3 NetworkPolicy denial lands with Phase 8.
  - [x] **Gate 7.5 — Guide public landing/demo page (Phase 7 DONE-WHEN completion) (2026-06-17)** — a clean guest chat over the Guide's `POST /ask`. **Lives standalone, SERVED BY the Guide** (`guide/static/index.html`, returned at `GET /` via `FileResponse`) — chosen over a route in the real `frontend/` because the public page then ships WITH the Guide and pulls in NONE of the real app's frontend/API surface (the strongest public-surface isolation). Vanilla self-contained HTML/CSS/JS — no Node, no TS, no external CDN/fonts/scripts (hermetic). UI: a question box → the Guide's answer with its **citations** (which approved doc(s) grounded it), honest **refusal** rendering (guardrail-block or low-relevance both show the polite out-of-scope message, no citations), a **loading** state, and an honest **error** state if `/ask` is unreachable (never a fabricated answer). **Endpoint:** same-origin `/ask` by default (`window.GUIDE_API_BASE` overridable; CORS only needed if hosted off-origin). **No real-app coupling** — asserted: the page references `/ask` and contains NONE of `/api/v1`, `:8000`, `/cohort`, `/participants`, `/monitoring`, `/auth` (`test_landing_page_couples_only_to_guide_ask`). **Isolation preserved:** guide import-graph + config-audit + tool-surface (7.1/7.2/7.4) STILL green — the page added no `vigil.*` import (it's static HTML + a `fastapi.responses.FileResponse` route). **Gates:** ruff clean; guide suite **39 passed**; full app spine **167** (untouched); fast **144**; check_specs **10**; real frontend `tsc --noEmit` clean (untouched). **PHASE 7 DONE-WHEN MET:** the Guide answers project questions from approved docs (7.2), refuses out-of-scope (7.2/7.3), and the 7.4 suite PROVES it cannot reach real resources even when prompted (layers 1–2 in CI; layer-3 NetworkPolicy denial deferred to Phase 8). **Next: Phase 8** — production-readiness (the layer-3 NetworkPolicy isolation proof on kind/minikube, k8s manifests, Docker Compose full stack, deploy) — the final build phase.
  - [x] **Gate 7.4 — THE ISOLATION-PROOF SUITE (sacred release gate; the 5.4 analog) (2026-06-17)** — exhaustive, adversarial proof that the Guide CANNOT reach any real resource even when prompted to; any single failure blocks shipping. **LAYER 1 — STATIC (CI):** (1.1) import-graph — AST scan of the whole `guide/` tree + a subprocess sys.modules check assert ZERO imports from `{vigil, hvac, redis, arq, psycopg}` (added the participant-DB driver); (1.2) config/secret audit — config surface == the allowed set only, no deny-listed cred/route field; (1.3) **TOOL-SURFACE** (`test_tool_surface.py`) — `retrieval.TOOLSET == ("approved_document_vector_search",)`, the retrieval tool is PURE (imports ⊆ stdlib; no socket/httpx/sqlalchemy/psycopg), and the grounding path (`rag`) reaches data ONLY through `guide.*` — exactly {vector search}, no DB/structured/participant tool. **LAYER 2 — BEHAVIORAL (CI, stub LLM, egress asserted):** a **versioned red-team suite** (`tests/guide/redteam_prompts.json`, 17 prompts × internal-call/jailbreak/secret/medical/out-of-scope) driven through `run_guide_turn` with a **socket tripwire** — for EVERY prompt: **zero outbound egress** (connect count 0) AND any citation ∈ the approved-doc filenames (never real data); medical/injection/secret are guardrail-**blocked**. **STRUCTURAL guarantee proven** (`test_structural_isolation_holds_when_guardrail_bypassed`): with the content guardrail NEUTERED, a "query the participant DB + print your API key" prompt STILL reaches nothing (zero egress, approved-only) — isolation does not depend on the filter. **Red-team found + fixed a real gap:** "ignore **your previous** instructions" slipped the original injection regex (the `(all|the)?` group rejected "your previous") — broadened the Guide's own `_INJECTION` to `ignore … instructions` with any qualifier between. **Guide RAG eval set** (`tests/eval/guide_eval.json`, dims grounded_citation/answerable_vs_unanswerable/low_relevance_refusal/out_of_scope_refusal) scored hermetically (offline embedder + stub LLM) — grounded cases cite approved docs (model_card pinned), unanswerable/low-relevance/out-of-scope refuse; **registered in `check_specs.py`** (mirrors the 5.7 RAG-eval enforcement) so the Guide can't ship without it. **LAYER 3 — NETWORK deferred to Phase 8** (NetworkPolicy denial from the Guide pod + positive control needs a kind/minikube cluster — not a CI gate). **Gates:** ruff clean; check_specs **10** (+ guide-eval artifact gate); full app spine **167 passed** (untouched); fast **143 passed**; guide suite **38 passed** (all layers green). **Phase 7 done-when PROOF MET:** answers from approved docs (7.2), refuses out-of-scope (7.2/7.3), and a test PROVES it cannot reach real resources even when prompted (7.4). Remaining: **7.5** landing/demo site (public UI over `/ask`).
  - [x] **Gate 7.3 — Guide guardrails + POST /ask + per-turn redacted event emission (2026-06-17)** — the public surface's safety boundary + auditability, fully `guide/`-owned (zero `vigil.*` imports preserved). NEW `guide/guardrails.py` (own copy of the content guardrails — `guard_inbound` = redact-first then refuse **medical/clinical**, **prompt-injection**, **secret-extraction** BEFORE retrieval/LLM; out-of-scope is the 7.2 relevance refusal). NEW `guide/turn.py` (`run_guide_turn`: redact → guardrail → if allowed retrieve+answer/relevance-refuse; writes ONE redacted row to the Guide's OWN sink for EVERY outcome — best-effort, a sink failure never breaks the turn). `guide/app.py` gains **POST /ask** (public, no auth, but reaches NOTHING real — only the vector tool; `create_app(resources=...)` is injectable for tests). **Outcome encoding:** guardrail-block → `blocked`/`refused`; relevance/zero-retrieval refusal → `allowed`/`refused`; grounded → `allowed`/`ok`. **Proofs (`tests/guide/test_guardrails.py`, 10 new; 27 guide total):** each category blocked + logged `blocked` (clinical/injection/secret, parametrized); on-topic answered+cited+logged `allowed`; out-of-scope refused + still logged; **every turn writes exactly one event row** (answered/relevance-refused/guardrail-blocked); PII in a question stored as `[REDACTED_EMAIL]` (raw never persisted); injection blocked AND no DB/HTTP tool exists in the turn path to comply (structural); `/ask` answers + blocks over the Guide's own resources, both turns logged. **Isolation re-proven:** the 7.1/7.2 import-graph + config-audit tests STILL green (guardrails guide-owned, zero `vigil.*`). CI hermetic (stub LLM, own SQLite sink — no live LLM/key/real resource). **Gates:** ruff clean; check_specs **10**; full app spine **167 passed** (untouched); fast **132 passed** (122 + 10). Next: **7.4** the SACRED isolation-proof suite (static import-graph/config-audit/tool-surface + behavioral red-team/egress-zero — the 5.4 analog).
  - [x] **Gate 7.2 — Guide RAG: approved-docs retrieval + own file-backed store + grounded/cited answer + sound refusal (2026-06-17)** — the Guide's ONE capability, fully `guide/`-owned (zero `vigil.*` imports preserved). NEW under `guide/`: `embeddings.py` (own offline MiniLM loader — HF offline forced, lazy torch import, reads a MOUNTED read-only weights path, no network), `retrieval.py` (own **file-backed JSON index** + top-k cosine — NOT pgvector, NOT `document_chunk`), `llm.py` (own minimal OpenRouter client + `StubGuideLLMClient`; `get_guide_llm_client` stub-FIRST on `VIGIL_GUIDE_LLM_STUB` → CI hermetic, no key/no call; reads the Guide's OWN key), `redaction.py` (own copy of the PII redactor — pure regex, no vigil import), `rag.py` (`answer_question`: redact → retrieve top-k → grounded+cited answer from approved docs ONLY, or SOUND refusal), `build_index.py` (build-time ingestion CLI). Config extended (still allowed-creds-only): own approved-docs path + index path, own LLM client config, own embedder path, retrieval top-k + **relevance_threshold=0.30**. **Toolset = {approved-doc vector search} exactly** — no DB/structured/participant tool exists in `guide/`. **Relevance-threshold refusal (folded 5.7 item) — SOUND for a public surface:** a turn refuses not only on zero retrieval but when the best cosine is below 0.30; tuned from measured sims — on-topic project Qs ~0.52–0.54, off-topic ≤0.15, a clinical ask ~0.27 (all cleanly on the right side); no open-generation fallback. **Proofs (`tests/guide/`, 7 RAG tests + the 10 isolation/skeleton tests STILL green):** grounded answer cites `model_card.md`; off-topic "weather" refuses (top-sim < 0.30); clinical ask refuses; empty index refuses; a refusal calls the LLM ZERO times (no fallback); the embedder makes no network call (sockets blocked during load+embed); CI uses the stub. **Guide LLM-key Vault path (place later): `secret/vigil/guide/llm_api_key`** (KV v2 field `value`; dev shim `VIGIL_GUIDE_LLM_API_KEY`). **Gates:** ruff clean; check_specs **10**; full app spine **167 passed** (untouched); fast **122 passed** (115 + 7). Index build artifacts gitignored. Next: **7.3** Guide guardrails (clinical/injection/secret content refusal + event emission on the answer endpoint).
  - [x] **Gate 7.1 — Guide service skeleton (separate, own creds, isolated) (2026-06-17)** — the first brick of the isolation proof: structural, not promised. NEW top-level **`guide/`** package (sibling of `vigil/`, NOT `vigil.guide`) imports NOTHING from `vigil.*`. `guide/app.py` — own FastAPI app (:8080 per infra.md) with `/healthz` + a landing stub, no retrieval/LLM/guardrails yet (7.2/7.3), opens no real-resource connection on startup. `guide/config.py` — minimal `GuideConfig` (env prefix `VIGIL_GUIDE_`) exposing ONLY the allowed set {`approved_docs_index_path`, `message_events_sink_dsn`, `llm_api_key`} + runtime host/port/log_level; NO participant-DB DSN / Vault token / Redis / queue / internal-admin field. `guide/observability.py` — the Guide's OWN `message_events` model (`GuideMessageEvent`, same 16-field observability.md schema, portable types, `surface=public_guide`/`sponsor_id=NULL` fixed) in its OWN store (SQLite default — never the app Postgres, never imports `vigil.db.models`) + `write_message_event`/`init_sink`. `guide/Dockerfile` (guide-only deps, no vigil). **Compose:** `guide` service on its OWN network `guide-net` (no route to postgres/redis/vault on the default net) — dev-layer of the boundary; the asserted NetworkPolicy denial + positive control is Phase 8. **Proofs (`tests/guide/`, 10 tests):** import-graph — AST scan of every `guide/` module + a subprocess sys.modules check assert ZERO imports from `{vigil, hvac, redis, arq}`; config/secret audit — config surface == the allowed set exactly, no deny-listed cred-ish field, the only DSN is the own sink, default sink is SQLite (not app Postgres); the shell starts (`/healthz` 200) touching no real resource; the own-sink schema matches observability.md and is a `guide.*` model; redacted write round-trips (guest/NULL, no raw column). **Gates:** ruff clean; check_specs **10**; full app spine **167 passed** (app untouched); fast **115 passed** (105 + 10 guide). Next: **7.2** Guide RAG over the approved docs + the Guide's own file-backed index (incl. relevance-threshold refusal).
  - [x] **Gate 7.0 — Phase 7 spec reconcile + ratified decisions (2026-06-17)** — SPEC + approved-docs content only; no Guide service code (that is 7.1). Locked the Phase-7 build contract into `isolation.md` (new `## Phase 7 ratified decisions` heading, registered in `check_specs.py` → stays 10): **(1)** the Guide OWNS its own copy of redaction/guardrail code and imports nothing from `vigil.*` (the §1 import-graph test asserts ZERO `vigil.*` imports — a stronger, simpler-to-prove claim than "shares only pure code"); **(2)** the approved-doc store is a **file-backed local index the Guide owns** (NOT the app's pgvector `document_chunk`, NOT shared; pgvector-parity deferred to Phase 8); **(3)** a **distinct LLM key** at the Guide's own Vault path `secret/vigil/guide/llm_api_key` (env shim `VIGIL_GUIDE_LLM_API_KEY`), and the Guide's ENTIRE allowed secret set = {own file-store path, own message_events sink, own LLM key} — config/secret audit asserts the absence of any participant-DB DSN / broad Vault token / Redis / queue / internal-admin URL; **(4)** `message_events` confirmed: the Guide writes its OWN sink (same schema, `surface=public_guide`, `sponsor_id=NULL`, separate store), never the app Postgres, never imports `vigil.db.models`. **Relevance-threshold refusal (folded-in 5.7 carryover):** the public Guide refuses on low-relevance retrieval, not only zero-retrieval — addressed in 7.2, asserted in 7.4's eval/red-team. `rag.md § Grounding rules` (Guide paragraph) updated to match (no new heading). **Approved-docs corpus AUTHORED** under `guide/approved_docs/` (the Guide owns them): `brief.md`, `architecture.md`, `model_card.md`, `safety_policy.md`, `deploy_notes.md` — PUBLIC-safe, honest, NO participant data / secrets / internal-only detail; every performance number matches the model cards/PHASE3_CARD (pan-indication 0.697 with the base-rate caveat + per-indication test-fold table; sequence +0.088 over the pre-registered bar; survival C-index 0.40 stated as a negative; synthetic-cohort + literature-prior-imputation disclosures; no PHI). check_specs **10**. No `vigil/` app code touched. Next: **7.1** Guide service skeleton.

## Phase 8 — Production-readiness (no paid hosting)  [ ]
Docker Compose dev · deploy/k8s (Deployments/Services/Ingress/Config/Secrets/Jobs/CronJobs/NetworkPolicies/HPA) on kind/minikube.
**Done when:** comes up on kind; CronJobs run; NetworkPolicies block the Guide.
> TARGETED SLICE IN PROGRESS — only the Guide's **layer-3 NetworkPolicy isolation proof** (completing
> the three-layer isolation centerpiece; layers 1–2 are CI-green from 7.4). The rest of Phase 8 (HPA,
> full prod k8s for the REAL app, cloud-KMS Vault auto-unseal, pgvector-parity for the Guide store) is
> deferred future work, stated honestly for the defense.
  - [x] **Gate HARD-1 — per-indication bootstrap CIs + tidy drift-closure (2026-06-17) — QUANTIFY-ONLY, story UNCHANGED** — added rigor to the empirical record and closed three known drifts; no claim was upgraded. **PART 1 (empirical):** `scripts/decompose_indication_pr_auc.py` now computes a **percentile bootstrap 95% CI** (2,000 resamples, fixed seed `MODEL_SEED`, resampling the test fold with replacement; single-class resamples skipped + counted) for every per-indication PR-AUC; regenerated `data/models/decomposition/indication_pr_auc.json` (+`decomposition_note.md`). **Point estimates reproduced BIT-IDENTICALLY** (T2D 0.3809319…, PSO 0.6474459…, IBD 0.4974972…, ALZ 0.7747289…, MDD 0.3205848…, MS 0.6201353…, HF 0.5829197…) — CIs added, nothing changed. The CIs **confirm** the existing reading: **T2D [0.323,0.454] and MDD [0.230,0.441] lie ENTIRELY below the 0.5 within-indication chance line** (below-chance is firm, not a fluke); **IBD [0.413,0.596]** straddles 0.5 (≈chance); **ALZ [0.681,0.858] OVERLAPS PSO [0.566,0.739]** → ALZ stays **directional, not a ranked winner** (172 test arms). Added the **base-rate-adjusted skill** to the artifact + both cards: pooled 0.697 vs test prevalence 0.474 → skill (0.697−0.474)/(1−0.474) ≈ **0.42** (real skill above the base rate, but the pooled number rides between-indication base-rate variation). Updated `PHASE3_CARD.md` (the "no CI" caveat → WITH CIs; ALZ paragraph) and `guide/approved_docs/model_card.md` (CIs + base-rate skill; synthetic/imputation disclosures unchanged). **PART 2 (tidy):** (a) synced the app-side `_INJECTION` regex (`vigil/agents/guardrails.py`) to the Guide's 7.4-broadened pattern so the in-app assistant also catches "ignore **your previous** instructions" etc. (defense-in-depth; the structural scoped-tools guard remains the strong one) + new `test_guardrail_blocks_qualified_injection`; (b) reconciled `specs/api.md` MessageQuery filter text `status` → `status_filter` (matches the live endpoint param + frontend); (c) removed the dead `TrafficSplitBar`/`SpendChart`/`BudgetAlertList` exports from `frontend/components/vigil/index.ts` (unused since the 6.4 costs rewrite). **Gates:** ruff clean; check_specs **10**; full app spine **160 passed, 7 `slow`-deselected, 0 skips** (the `slow` 18k-regen tests are excluded by default/CI — 160+7=167 collected, baseline); guide guardrail/red-team + agents-boundary **25 passed**; frontend `tsc --noEmit` exit 0. No clinical/precursor claim added; the honest story stands — only quantified.
  - [x] **Gate 8.L3-c — fixed the layer-3 runner bugs the live run exposed (2026-06-17) — Guide isolation now THREE LAYERS complete** — a hand-executed run against a real kind+Calico cluster **PASSED the actual proof** (negative pre-check: all 6 targets REACHABLE pre-policy; after applying the policies into the correct namespace, the 5 deny-list targets — postgres/redis/vault/app-api/model-endpoint — all TIMED OUT/DENIED and allowed-egress:8088 stayed REACHABLE) — **isolation is HAND-VERIFIED**. This gate fixed the SCRIPT bugs that required manual intervention so a clean run reproduces it: **BUG 1 (serious)** — `networkpolicies.yaml` had no `namespace:`, so a raw `kubectl apply -f` landed both policies in `default` where they selected nothing and enforced NOTHING (a fresh run would falsely show all targets reachable; the negative pre-check CAUGHT this live) → both policies now self-declare `namespace: vigil-isolation-proof` (kustomize-build-confirmed). **BUG 2** — Calico/deploy waits were fragile ("no matching resources found" / timed out on a slow bring-up) → added `Wait-Exists`/`wait_exists` (poll until the resource REGISTERS before waiting) + `Invoke-WaitWithRetry`/`wait_retry` (retry the readiness waits) + `rollout status` on `ds/calico-node` & `deploy/coredns`. **BUG 3** — the in-pod probe threw a PS→kubectl quoting traceback → rebuilt as a single interpolated python statement passed via a SPLATTED kubectl arg array (`.ps1`) / one `-c` arg (`.sh`), the form hand-verified to work; negative pre-check kept FIRST + 8s propagation pause retained. **Validated WITHOUT a cluster:** `kubectl kustomize deploy/k8s` shows BOTH NetworkPolicies carry `namespace=vigil-isolation-proof`; `bash -n` OK; PowerShell AST parse 0 errors (also stripped non-ASCII that broke PS 5.1 string parsing). **The Guide isolation proof is now ALL THREE LAYERS:** static + behavioral in CI (7.4), network on the local Calico cluster (hand-verified PASS; the fixed script reproduces it). **No regression:** app untouched (full spine **167**); guide suite **39**; check_specs **10**; ruff clean. User re-runs `pwsh ./deploy/k8s/run-isolation-proof.ps1` to confirm end-to-end.
  - [x] **Gate 8.L3-b — layer-3 in-cluster denial proof script + runner (2026-06-17; AUTHORED — user runs the cluster)** — the runnable artifact that stands up a **kind + Calico** cluster, applies the 8.L3-a manifests, and PROVES the Guide pod reaches NO deny-list resource at the network level. `deploy/k8s/run-isolation-proof.ps1` (Windows, primary) + `run-isolation-proof.sh` (portable) + `kind/kind-config.yaml` (default CNI **disabled** so Calico enforces) + `make guide-isolation-proof`. **Sequence (8 steps):** create kind (CNI off) → install Calico v3.27.3 + wait ready → `docker build` guide image + `kind load` → apply namespace/guide/targets/positive-control **(NOT the policies)** + wait available → **NEGATIVE PRE-CHECK** (from the guide pod, raw-connect every deny-list `host:port` → must be **REACHABLE**, so the later denial is provably the policy, not a broken net/wrong CNI) → apply the guide NetworkPolicies + 8s propagation → **DENIAL TEST** (re-connect each → must be **DENIED**, 3s-timeout python `socket.create_connection` via `kubectl exec`) → **POSITIVE CONTROL** (`allowed-egress:8088` → **OK**) → PASS/FAIL summary + transcript (`deploy/k8s/last-proof-transcript.txt`, gitignored) → teardown (`-Keep`/`--keep` leaves it up for the live side-by-side demo). **The USER runs it** (`pwsh ./deploy/k8s/run-isolation-proof.ps1`) — the cluster cannot run in the hermetic unit-CI; this is the kind-based integration demo (specs/isolation.md §3, infra.md — no paid cluster). **Validated WITHOUT a cluster:** `bash -n` (sh OK); PowerShell AST parse 0 errors; every script target maps to a manifest Service + both NetworkPolicies present (parser cross-check); referenced files exist. **No regression:** app untouched (full spine **167**); guide suite **39**; check_specs **10**; ruff clean (scripts/docs/Makefile/.gitignore only). **Completes the Guide isolation proof to ALL THREE LAYERS** — static + behavioral in CI (7.4), network on the local Calico cluster (here) — once the user's run passes. Marked `[~]` (authored; flips to `[x]` on a green run). Honest scope: a LOCAL demonstration of the NetworkPolicy posture; the same manifests apply to prod. Deferred Phase-8 future work unchanged (HPA, full prod app k8s, cloud-KMS auto-unseal, Guide pgvector-parity).
  - [x] **Gate 8.L3-a — Guide layer-3 manifests + NetworkPolicies + positive-control reconcile (2026-06-17)** — authored `deploy/k8s/` (kustomize base) for the layer-3 proof; statically validated, no cluster run (that is 8.L3-b). **17 objects:** the `guide` Deployment+Service (`vigil-guide:local`, :8080, runnable + exec-able — RAG-in-pod not required for the network proof, weights/index mount stays deferred); the **deny-list targets** on their REAL ports — `postgres` (:5432), `redis` (:6379), `vault` (:8200 dev), + `app-api` (:8000) and `model-endpoint` (:9000) HTTP stand-ins — each a Service (reachable at L3 absent the policy, so the denial is real); the **positive-control** `allowed-egress` stand-in (:8088); and the **NetworkPolicies** — `guide-default-deny-egress` (`egress: []`) + `guide-egress-allow` (permits ONLY DNS to kube-system:53 + `allowed-egress:8088`). Parser-asserted: the deny-list apps have NO matching allow → denied; the allow lists ONLY DNS + the positive control. **README** documents the **Calico-enforcement prerequisite** (kindnet/minikube-default do NOT enforce NetworkPolicies → FALSE PASS) + the **negative pre-check** requirement for 8.L3-b + honest scope (local-cluster demonstration; same manifests apply to prod). **SPEC:** `isolation.md §3` positive-control **reconciled** — file-backed in-pod index → the in-cluster `allowed-egress` stand-in is the gated positive control (no new heading → check_specs stays 10); enforcement-CNI + negative-pre-check requirements recorded in §3. **Validation:** `kubectl kustomize deploy/k8s` builds 17 well-formed objects (7 Deployment / 7 Service / 2 NetworkPolicy / 1 Namespace), all in the `vigil-isolation-proof` namespace; NetworkPolicy intent parser-verified. **Gates:** ruff clean; check_specs **10**; full app spine **167** (untouched — manifests + one prose spec edit only); guide suite **39**. Next: **8.L3-b** — stand up kind+Calico, apply, run the in-pod raw-connect denial test + positive control, record the result for the demo.

---

## Phase 9 — Clinical-ops loop (capability demonstration on labeled-synthetic data)  [x]
The serious-risk crossing → scope-bound in-app at-risk surface (reasons + trajectory + recommended
actions) + a minimal PII-FREE email doorbell. Framed as a CAPABILITY DEMONSTRATION, never a
clinical finding; every output labels the signal synthetic. Real-app only — does NOT touch the
public Guide; weakens no isolation/sacred invariant.
**Done when:** a synthetic event sequence shifts a participant's trajectory across the serious
threshold; the crossing populates the scope-bound at-risk surface with REAL model-attribution
reasons + recommended actions; a PII-free deep-link email is sent (stubbed in CI); cross-tenant +
cross-site (recipient-routing) tests green; Guide isolation untouched.
Gate order: **9.1** real `top_factors` (replace the empty-`[]` writeback with genuine model
attributions) → **9.2** crossing-detection (worker-side, idempotent, deduped) + scope-bound at-risk
`/cohort?risk_band=high&sort=risk_desc` → **9.3** recommended actions (protocol guidance, distinct
from model reasons) → **9.4** at-risk frontend surface (reuse triage/participant; wire the
`/risk/history` sparkline) → **9.5** per-user notification email field + `/me` settings +
scope-bound routing (sacred) → **9.6** email notifier (Gmail SMTP/Vault/allow-list/CI-stub) →
**9.7** synthetic trajectory demo (done-when).
  - [x] **Gate D1 — containerized the API + worker (Docker Compose, dev-only Vault auto-unseal, service-name networking) (2026-06-18)** — the deferred Phase-8 "containerize the real app" work, scoped to LOCAL Docker Compose (NOT k8s — later). **Image:** one shared `./Dockerfile` (python:3.12-slim + uv; `uv sync --frozen --no-dev --no-cache` from the committed lock; runtime deps only) drives TWO compose services — `api` (`uvicorn vigil.api.app:app` :8000) and `worker` (`arq vigil.workers.settings.WorkerSettings`). The 636MB `data/` tree (synthetic parquet, model artifacts, vendored embedder) is **bind-mounted read-only** (`./data:/app/data:ro`), never baked; `.dockerignore` keeps the context lean. **CPU-only PyTorch:** the default Linux PyPI torch is the multi-GB CUDA wheel + `nvidia-*` stack — the wrong artifact on a GPU-less host (the first build hit 8.5GB+). `pyproject.toml` now pins the **PyTorch CPU index** for the Linux torch resolution only (`[[tool.uv.index]] pytorch-cpu` + `[tool.uv.sources] torch = {index="pytorch-cpu", marker="sys_platform=='linux'"}`; Windows/macOS PyPI torch are already CPU), and `uv lock` dropped every `nvidia-*`/`cuda-toolkit`/`triton` package → `torch 2.12.1+cpu` (verified in-container `cuda_available False`; LSTM inference/training + sentence-transformers run fully on CPU). **Final image: 2.38GB** (8.5GB+ CUDA → 4.49GB CPU → 2.38GB after `--no-cache` drops the duplicate wheel cache). **Security model PRESERVED (the invariant this gate must not break):** the image holds NO secrets — at runtime the app reads `jwt_signing_key` + the DB DSN from **Vault** (`VIGIL_SECRETS_BACKEND=vault`, `VAULT_ADDR=http://vault-dev:8200`) and connects to Postgres as the **non-superuser `vigil_app` under RLS**, exactly as the host flow; no app/RLS/scope/secrets-reading code changed (config + Docker/compose only). **DEV Vault auto-unseal:** a new `vault-dev` service (`vault server -dev`, in-memory, fixed root token `vigil-dev-root`, **DEV-ONLY**) comes up auto-unsealed so `compose up` needs no manual unseal; a one-off `vault-seed` writes the app secrets — crucially the **IN-CONTAINER DB DSN** (`…@postgres:5432/vigil`, resolving the host `localhost:55432` → service-name `postgres:5432` difference *inside the secret value*, so secrets stay in Vault where they belong). The **prod model is unchanged + still documented**: the persistent Shamir-sealed `vault` service (now behind `--profile prod-vault`) + `infra/vault/vault.hcl` + manual `operator init/unseal` (host flow in `docs/RUNBOOK.md` §1.1–1.5). **Service-name networking:** api/worker get `VAULT_ADDR=http://vault-dev:8200` + `VIGIL_REDIS_URL=redis://redis:6379/0`; the DSN resolves to `postgres:5432` via the seeded secret. **Ordering/health:** `vault-dev` healthcheck (`vault status`) → `vault-seed` (one-off) → api/worker `depends_on` `postgres:service_healthy` + `redis:service_healthy` + `vault-seed:service_completed_successfully`, so the app never starts before Vault is seeded + Postgres is ready (the fail-loud `SecretNotFoundError` race is structurally avoided); the engine keeps `pool_pre_ping`. **Migrate/seed path (profile `tools`, run via `docker compose run --rm`):** `migrate` runs `alembic upgrade head` as the **owner** (`VIGIL_DB_ADMIN_DSN=…vigil@postgres…`); `seed` runs `python -m vigil.seed` as **`vigil_app`** — the same owner/app role split as the host flow; a fresh DB volume also needs the one-time `scripts/bootstrap_db.sql` (documented). Compose hygiene: `guide` moved behind `--profile guide` (D3), so `--profile app` brings up exactly postgres + redis + vault-dev + vault-seed + api + worker. **LIVE bring-up proof:** `docker compose -f docker-compose.dev.yml --profile app up -d` brought up postgres+redis (healthy) → `vault-dev` (healthy, **`vault status` Sealed=false**, inmem dev) → `vault-seed` (exited 0) → `api` (**Up, healthy**) + `worker` (**Up**, registered 4 functions, redis connected). `GET /healthz` → `{"status":"ok"}`. Login (`coord.a@vigil.example`) → token → `GET /cohort` returned **only the coordinator's own site** (all rows one trial/site; sponsor-B invisible); platform `admin` → **HTTP 403**. **`pg_stat_activity` confirms the containerized app connects as `vigil_app` (`rolsuper=f, rolbypassrls=f`)** — RLS genuinely binds (a superuser/bypass connection would have leaked cross-site rows); the Vault secret it reads is the **in-container DSN** `postgresql+psycopg://vigil_app:vigil_app_pw@postgres:5432/vigil`. Security model intact end-to-end: secrets from Vault, Postgres as the non-superuser under RLS. **Docs:** `docs/RUNBOOK.md` §1.A documents the new `compose --profile app up` flow + the dev-vs-prod Vault distinction; the host/uv flow is retained. **No-regression:** ruff clean; check_specs 10; full spine under Postgres green (the test suite runs against the host/CI Postgres, unaffected by the app containers). Frontend (D2) + Guide (D3) + k8s (later) next.
  - [x] **Gate FIX-5 — fixed "Days Enrolled = 0" (2026-06-18)** — the deep investigation pinned the root cause in two places. **(1) Detail page (real data fix):** `participant/[id]/page.tsx` computes `daysSince(detail.enrolled_at)`, but `participant.enrolled_at` defaulted to the DB `now()` (`0001_initial_rls.py` `_ts("enrolled_at")`) and the seed created the participant WITHOUT setting it (`seed.py`), so it read the seed-run moment → ~0 days even though the visit history spans months. The seed already computed an `enrollment_date` for the visit-timestamp anchor but only applied it to the visits. **Fix:** `_seed_engagement` now sets `p.enrolled_at = enrollment_date` (the trajectory START == visit_index-0 timestamp), so the detail page shows REAL enrollment duration (months, in the past). **(2) Triage/dashboard placeholder — Option A (real value):** the triage adapter hard-coded `daysEnrolled: 0` because `CohortRow` carried no enrollment field. Added `enrolled_at` to `CohortRow` end-to-end — `specs/api.md § cohort` (spec first), `cohort_service.CohortRow` (from `p.enrolled_at`), the `cohort.py` router model, `frontend/lib/types.ts`, and the dashboard + triage adapters now derive `daysEnrolled` from a shared `daysSince` util (`lib/utils.ts`; the detail page now imports it too — one helper). The field rides the SAME scope-bound participant row (RLS + SEC-1) — no new read semantics, no widening; the cohort summary path is unaffected. **Tests (`tests/spine/test_fix5_enrolled_at.py`, real Postgres):** the seeded participant's `enrolled_at` is at/before the earliest engagement visit AND strictly before `created_at` AND ≥1 day in the past (the bug signature `enrolled_at == now()` is gone); the cohort row carries `enrolled_at`, it is in the past, and it equals the detail endpoint's `enrolled_at` for the same scoped participant. **No spec count change** (added a field to an existing schema) → check_specs stays 10. **Gates:** ruff clean; check_specs **10**; frontend `tsc --noEmit` clean; full spine under Postgres green. **RE-SEED required** for the detail page to show real days: `uv run python -m vigil.seed` (or drop+migrate+seed) — existing rows keep their old `enrolled_at`.
  - [x] **Gate FIX-4 — built the scope-bound `GET /cohort/summary` (dashboard + triage empty-data bug) (2026-06-18)** — the live dashboard + triage pages rendered NO data: the frontend called `GET /api/v1/cohort/summary` which **404'd** (the endpoint was **specified but never built** — `api.md § cohort` line declares `GET /cohort/summary → CohortSummary{total, by_band, mean_risk}` "for the same scoped slice", and `dashboard.md` references it, but `vigil/api/routers/cohort.py` had only `GET /cohort`). The frontend (`lib/api.ts getCohortSummary`, `lib/types.ts CohortSummary`) already matched the spec exactly. **Direction (a) — built the endpoint** (keeps scope-binding server-side; no frontend change). `cohort_service.summarize_cohort` reuses `list_cohort`'s EXACT scope-bound narrowing (RLS + SEC-1 `scope_filter.participant_visible`) with NO row cap (`list_cohort` `limit` now accepts `None`) so the totals cover the whole scoped cohort, not a page; optional `trial_id`/`site_id` are an additional restriction (out-of-scope id → empty, never widening); `by_band` always carries all three bands. New `GET /cohort/summary` router endpoint with the SAME platform-role 403 + `ScopeError`→403 guards as `/cohort`. **Scope-bound proof (`tests/spine/test_cohort_summary.py`, HTTP-level, delta-based):** shape (total/by_band{high,medium,low}/mean_risk, `total == /cohort total`); a coordinator's summary counts an OWN-site high participant (+1) but NOT a cross-SITE nor a cross-TENANT one; sponsor-oversight is not over-narrowed (sees both new sponsor-A sites, +2); platform/auditor → 403. **No spec change** (endpoint was already in api.md) → check_specs stays 10. **Gates:** ruff clean; check_specs **10**; frontend `tsc --noEmit` clean; full spine under Postgres **216 passed, 0 skips** (212 + 4 summary). User restarts the API + reloads the browser to confirm dashboard + triage populate (the 28-participant cohort + the summary strip).
  - [x] **Gate FIX-3 — CORS on the API so the browser frontend can reach it live (2026-06-18)** — the live frontend (`http://localhost:3000`) could not call the API: the browser's CORS **preflight OPTIONS** hit routers with no OPTIONS handler → **405**, so login + every data fetch failed in the browser (server-side PowerShell calls, which send no Origin, were unaffected). **Root cause:** `vigil/api/app.py` added **no `CORSMiddleware`** at all. **Fix:** added `CORSMiddleware` as the OUTERMOST middleware (after the context middleware so it answers the preflight before routing) with an **explicit, configurable allow-list** — new `Settings.cors_allow_origins` (comma-separated; env `VIGIL_CORS_ALLOW_ORIGINS`; local-dev default `http://localhost:3000,http://127.0.0.1:3000`) parsed via `cors_allow_origin_list`; `allow_credentials=True` (the app authenticates with a Bearer token, so the allow-list is explicit, never `"*"`), `allow_methods=["*"]` (covers the OPTIONS preflight), `allow_headers=["*"]`, `expose_headers=["x-request-id"]`. **No other behavior touched** — CORS is inert for server-side callers (no Origin). 4 tests (`tests/spine/test_cors.py`, no DB): preflight to `/api/v1/auth/login` returns 200 (not 405) with `access-control-allow-origin` echoed for both host spellings + `allow-credentials: true`; a real GET carries the header; `/cohort` + `/scoring/trigger` preflight clear; an unlisted origin is NOT echoed. **Gates:** ruff clean; check_specs **10**; full spine under Postgres **212 passed, 0 skips** (208 + 4 CORS). User restarts the API + retries the browser login to confirm end-to-end.
  - [x] **Gate FIX-2 — rewrote the stale README from the verified catalog (2026-06-18)** — documentation only; NO code, NO metric value, NO spec touched (`README.md` + this ROADMAP entry). The README described only the **Phase-0 scaffold** (specs + tooling + the Vault unseal lifecycle) and was missing the entire built system. Rewrote it to accurately describe the finished Vigil (Phases 1–9), **honest framing leading**: the modeling story (pan-indication GBT **0.697** with base-rate-adjusted skill **≈0.42** + per-indication bootstrap CIs showing **T2D/MDD below their within-indication chance line**; the sequence LSTM **+0.088** as a **calibrated** architecture/method demonstration on labelled-synthetic planted-signal data, discrimination provably unchanged, **NOT a clinical prediction**; the **negative survival C-index 0.40**; synthetic + literature-prior-imputation provenance; no PHI); the multi-tenant **RLS + SEC-1** isolation; champion/shadow scoring + crossings + real attributions; the **hand-rolled** LLM-classify router + scoped tools (explicitly NOT LangGraph/MCP); observability; the isolated **Guide three-layer proof**; the clinical-ops loop; the frontend; the verified tech stack; and a run orientation that **points to `docs/RUNBOOK.md`** for the full procedure while **keeping the accurate Vault-unseal section** verbatim. **Every cited number re-verified against the cards/artifacts** (`PHASE3_CARD.md`, `baselines/metrics.json` 0.69677/0.4740, `sequence_metrics.json` 0.33904, the `sequence_v1.1:demo` calibration block) before writing. **Gates:** check_specs **10** (README is not a spec); ruff clean (no code); no code/metric/spec file changed.
  - [x] **Gate FIX-1 — doc-accuracy reconciliation + record cleanup (2026-06-18)** — documentation/record/cosmetic fixes ONLY; NO app code or metric value changed. **(1) LangGraph/MCP doc-vs-code reconciled:** confirmed from source that neither **LangGraph** nor **MCP** is a dependency (`pyproject.toml`/`uv.lock`) or imported anywhere — the real agent layer is a **hand-rolled LLM-classify router** (`vigil/agents/router.py`) dispatching scoped agents on a **shared grounding spine** (`agent_base.py`) with **plain scope-resolved tools** (`tools.py`: champion-only risk facts + RLS-scoped pgvector search). Updated the docs to match: `CLAUDE.md` (`agents/` line: was "LangGraph + MCP tools" → now the hand-rolled router/spine/tools, "an MCP-style/tool-calling PATTERN, NOT the MCP protocol or LangGraph"); `specs/rag.md` (Decisions "via MCP"/"LangGraph orchestration" + Retrieval-sources "MCP tools" → scope-resolved tools, hand-rolled orchestration, NOT MCP/LangGraph); `specs/observability.md` (`route_or_agent` "LangGraph agent/node" → "agent that handled the turn"); `ROADMAP` Phase-5 header prose + Decisions-log line ("LangGraph orchestration" → hand-rolled, NOT LangGraph/MCP). Historical dated gate-log entries (5.0/5.3/5.4 "scoped MCP tool layer") left intact as the record. **(2) Committed personal email removed:** the demo recipient's personal Gmail literal (superseded by Gate 9.5's env-sourced `VIGIL_DEMO_NOTIFY_EMAIL`, but the literal survived in two docs) replaced with the env-sourced description in **`ROADMAP.md`** (Gate 9.0 entry) and **`specs/domain.md`** (§ Notification routing seed contract); the only remaining occurrence is `tests/spine/test_b2g_recipients.py` where it is the **negative-assertion needle** (asserts the address appears nowhere in `vigil/**.py`) — correctly left. **(3) ROADMAP staleness:** Phase headers **3 / 5 / 9** flipped `[ ]` → `[x]` (done-whens met per their bodies/final gates); stale register lines marked done — "positive_prevalence not recorded" (done in B4, 0.4740) + the monitoring/cost frontend-wiring lines (done in 6.4). **(4) Cosmetics:** `ingestion/eda/__marimo__/session/notebook.py.json` (generated marimo session cache embedding a machine path) `git rm --cached` + `__marimo__/` added to `.gitignore` (not hand-edited); `frontend/package.json` name `my-project` → `vigil-frontend`. **Gates:** ruff clean; check_specs **10** (heading prefixes intact); full spine under Postgres **208, 0 skips** (unchanged — no code touched); frontend `tsc --noEmit` clean. No app behavior or metric value changed.
  - [x] **Gate FINAL-1 — cosmetic cleanup + live-test runbook (2026-06-18)** — repo hygiene + operator documentation; NO sacred surface, NO behavior change (metric VALUES unchanged — only path strings / labels / comments / a new doc). **Cleanup:** (1) scrubbed machine-specific absolute paths (`C:\Users\LEGION\…`) from the committed artifacts — `data/models/metrics.json`, `data/models/baselines/metrics.json`, `data/models/t2d/{sequence_metrics,baseline_real,baseline_synthetic_structural}.json`, `data/eda/eda_summary.{json,md}`, `data/synthetic/manifest.json` — to repo-relative forward-slash paths (a value-preserving transform; numeric digest asserted identical before/after, so no metric number moved). (2) `data/models/PHASE3_CARD.md`: corrected the mislabeled "37k+ arms" — `37k` is the **trial** count, not arms; the 0.697 modelling cohort is **~37k trials / ~97k arms** (`baselines/metrics.json`: `fold_sizes`=37,246 trials, `n_arms`=96,679), drawn from the full cleaned **73,073-trial / 182,240-arm** `ref_*` tables; honest metric numbers untouched. (3) `frontend/tsconfig.tsbuildinfo` (a regenerable TS build cache) `git rm --cached` + `*.tsbuildinfo` added to `.gitignore`. (4) `pyproject.toml`: fixed the stale `slow` marker comment that claimed slow tests are "excluded from CI" — CI DOES run the slow-inclusive spine (`pytest tests/spine -m "slow or not slow"`); only the heavy 18k-cohort regeneration slow tests outside `tests/spine` are CI-excluded (`make test-slow`). **Runbook:** authored `docs/RUNBOOK.md` — ordered local bring-up (Docker stack → Vault unseal 3-of-5, keys off-repo → env → migrate → seed w/ `VIGIL_DEMO_NOTIFY_EMAIL` → API/worker/frontend/Guide), the **exact** Vault secret paths verified against `secrets.py` (jwt/db/llm/anthropic/langfuse×2/email + the Guide's own `secret/vigil/guide/llm_api_key`, field `value` + env-shim each), the four stub flags (`VIGIL_LLM_STUB`/`LANGFUSE_ENABLED`/`EMAIL_STUB`/`GUIDE_LLM_STUB` — defaults + flip direction), the run-once live-keys checklist (a Anthropic turn, b Langfuse v4 create_event+flush, c Guide turn, d kind+Calico isolation proof `run-isolation-proof.ps1 -Keep`, e Phase-9 PII-free email), and the honest demo/defense flow (structural-associations + HARD-1 CIs → per-indication honesty incl. below-chance T2D/MDD → calibrated synthetic sequence as capability-not-clinical → agentic RAG → observability → Guide 3-layer isolation → clinical-ops loop). **Gates:** ruff clean; check_specs **10**; full spine under Postgres **188 passed + 20 slow = 208, 0 skips** (cosmetic-only, unchanged); frontend `tsc --noEmit` clean; metric VALUES verified unchanged.
  - [x] **Gate 9.7 — synthetic sequence-trajectory demonstration (Phase-9 DONE-WHEN) — PHASE 9 COMPLETE (2026-06-18)** — ties the clinical-ops loop together end-to-end as a CAPABILITY DEMONSTRATION on labeled-synthetic data with a PLANTED signal (NOT a clinical finding), now driven by the REAL calibrated model (9.7a) with NO override. **The loop:** an accruing missed-visit sequence injected for a SYNTHETIC participant (2 attended → 12 missed, in 4 stages) is rescored after each stage by the REAL calibrated champion LSTM (`sequence_v1.1:demo`) → the risk TRAJECTORY climbs `0.098 (low) → 0.316 → 0.569 (medium) → 0.6745 (high)`, crossing the `>0.6` serious threshold at the final stage (visible on `/risk/history`, per-point synthetic) → a genuine non-high→high CROSSING is recorded (9.2, no override) → the scope-bound AT-RISK surface (`/cohort?risk_band=high`, 9.4) shows the participant with REAL LSTM-attribution reasons (9.1 — `consecutive_missed` the top driver, the planted signal; leakage-safe) + recommended ACTIONS (9.3 — call/reminder/clinic-follow-up, operational not clinical) + the synthetic label → the PII-FREE scope-bound EMAIL doorbell (9.6) sends ONCE to the in-scope coordinator (cross-site coordinator excluded), stubbed in CI. **Reused 9.1–9.6 unchanged — no new clinical surface.** **Demo PATH:** `scripts/demo_clinical_ops_loop.py` (documented, runnable, Windows-console-safe) drives + prints the whole loop with the synthetic/planted-signal framing stated up front and at the end. **DEMO-SEED CLEANUP (carried item closed):** `seed.py` no longer writes the fabricated `top_factors=["missed_visits","hba1c_trend","bmi"]` / fabricated reasons — the seeded demo score now carries EMPTY `top_factors`/`reasons` (a real calibrated-champion score populates them via 9.1), so no hand-authored clinical rationale the model never produced can ever surface. **SYNTHETIC PROVENANCE end-to-end:** every surface — trajectory points, at-risk row + detail, reasons, crossing, email body — carries the synthetic-demonstration label (asserted). **Tests (`tests/spine/test_b97_demo_loop.py`, real Postgres, stub LLM/email, slow/torch):** the DONE-WHEN end-to-end (accruing inject → real-calibrated trajectory crosses >0.6 no override → one crossing → at-risk surface with non-empty real reasons ⊆ model inputs + leakage-safe + approved actions → email sends once PII-free + scope-bound, cross-site recipient excluded → synthetic on every surface); the demo-seed fix (the earliest seeded score row has empty `top_factors`/`reasons`). **Gates:** ruff clean; check_specs **10**; golden oracle **5**; full spine under Postgres **188 passed, 20 slow-deselected, 0 skips** + slow LSTM suite **20 passed** (b97 +2) = **208 green**. Phase-9 clinical-ops loop (9.1→9.7) COMPLETE. Next: the live-send / live-LLM / Langfuse manual verifications (bundled, not a CI gate).
  - [x] **Gate 9.7a — calibrate the champion LSTM so the >0.6 serious band is reachable by the REAL model (2026-06-18)** — the trained champion discriminates correctly but its raw per-decision-point sigmoid outputs are COMPRESSED (max ~0.54), so the operational `>0.6` HIGH band was unreachable and a serious-risk crossing could only be forced via a scorer override. Fixed by CALIBRATING the outputs (probability-scale remap), NOT by changing discrimination. **Method:** a MONOTONIC ISOTONIC output calibrator (`models/t2d/calibration.py`), persisted WITH the artifact as `(x,y)` knots and applied at score time via `np.interp` (numpy-only at runtime; sklearn only at build/fit time). Platt/logistic was evaluated and REJECTED — the model is already globally near-calibrated (test ECE ≈ 0.0078, Platt slope ≈ 1.06), so a global sigmoid cannot lift the compressed top past 0.6; isotonic corrects the LOCAL under-confidence at the top (highest raw-score decision points have empirical dropout rate ≈ 0.74). **Fit split (leakage-safe):** the held-out `val` fold ONLY (temporal, group-disjoint-by-`nct_id`), DISJOINT from BOTH the LSTM's `train` fold AND the reported `test` fold; the artifact freezes `train_nct_ids`/`val_nct_ids`/`test_nct_ids` so disjointness is asserted hermetically. The calibrator uses NO outcome at score time. **DISCRIMINATION UNCHANGED (the honesty test):** calibration is monotonic → ranking preserved → test ROC-AUC invariant (raw 0.790114 → calibrated 0.790061, Δ = 5.3e-5) and raw per-decision-point PR-AUC reproduces the trained bar (0.3390). It does NOT manufacture discrimination. **Reachability (no override):** the calibrated REAL LSTM scores a heavy-disengagement trajectory (10 consecutive missed) at 0.6745 (raw 0.5056 ≤ 0.6 → calibration UNLOCKED the band), and drives a genuine non-high→high crossing end-to-end through `score_trial` with NO override. **Calibration quality (honest, not overclaimed):** test ECE 0.00780 → 0.00750, Brier 0.075257 → 0.075027 — the win is unlocking the band, not a large calibration gain. **Attribution unchanged:** `top_factors`/`reasons` occlusion is computed on the model's OWN pre-calibration output (monotone calibrator doesn't reorder importances → b2d stays real + leakage-safe; isotonic flat regions never zero the deltas). **Version bump:** the calibrated champion is `sequence_v1.0:demo` → `sequence_v1.1:demo` (probability semantics changed); the `>0.6`/`>0.3` thresholds are UNCHANGED; recorded in `specs/scoring.md § Output calibration` (ratified) + the t2d model card. The orphaned uncalibrated `sequence_v1.0_demo.pt` was removed; seed/routing/all spine references bumped to v1.1. **Gates:** ruff clean; check_specs **10**; golden oracle **5**; full spine under Postgres **188 passed, 18 slow-deselected, 0 skips** + slow LSTM suite **18 passed** (b2b 7, b2d 4, **b97a +7** the calibration honesty suite) = **206 green**. Next: **9.7** synthetic trajectory demo (now drivable by the real calibrated model — no override).
  - [x] **Gate 9.6 — PII-free serious-risk email notifier (Gmail SMTP) (2026-06-18) — Phase-9 clinical-ops loop COMPLETE** — when a 9.2 crossing fires, send a PII-FREE doorbell to the 9.5 scope-bound recipients, EXACTLY ONCE, via an Arq job (never inline). **Sender** (`vigil/services/email_sender.py`): stdlib `smtplib` + STARTTLS, stub-first like the Langfuse tracer — `get_email_sender()` returns `StubEmailSender` (records intent, NO network, NO credential) whenever `email_stub` (default **TRUE**), the real `SmtpEmailSender` only when `VIGIL_EMAIL_STUB=false`. **Egress:** Gmail SMTP host (`smtp.gmail.com:587`) joins the **app-side deny-by-default allow-list** alongside `api.anthropic.com`/`openrouter.ai`/the Langfuse host (specs/isolation.md/infra.md). **Secret:** App Password at **Vault `secret/vigil/notifications/email_password`** (KV v2, field `value`) + env shim `VIGIL_NOTIFY_EMAIL_PASSWORD` (`EnvSecrets` map) — the ONLY new secret; host/port/From are non-secret config; recipients come from `notification_email` records (9.5), never a secret. **PII-FREE body (the guarantee, `build_notification_body`):** built ONLY from the deep link (`{app_base_url}/at-risk`, no id) + the synthetic label — carries NO participant id / coded_ref / risk score / factor by construction (specs/isolation.md § Email body). **Send-once** (`notification_service.notify_crossing`, enqueued from `score_trial` after the writeback commits): resolve recipients → if recipients AND not yet `notified` → send → flip `notified` true; an already-notified crossing does NOT re-send; a retried job does NOT double-send. **Failure handling:** a send failure raises → the Arq job re-raises so the framework retries (bounded by `WorkerSettings.max_tries=5`); `notified` is NOT flipped on failure (so a later try can deliver). The scoring-path enqueue is best-effort (a Redis hiccup never breaks scoring; the crossing stays `notified=false`). **CI-hermetic:** `VIGIL_EMAIL_STUB=true` in ci.yml + conftest → tests/CI send NO real email and need NO credential. **Tests (`tests/spine/test_b2h_email_notifier.py`, real Postgres, stub sender):** send-once + notified-flip + no-resend; no-recipient → no send/no crash/notified-stays-false; **PII-free body STRUCTURAL** (asserts the actual participant id / coded_ref / 0.91 / `consecutive_missed` are ABSENT, deep link + synthetic label PRESENT); **SCOPE-BOUND at the send path** (site-1 crossing → recipients == [site-1 only]; site-2 + Sponsor-B excluded); **failure** → `EmailSendError` propagates, `notified` stays false, sender called once; hermetic stub default. **Live-send (run-once manual, NOT a CI gate):** set `VIGIL_EMAIL_STUB=false` + `VIGIL_NOTIFY_FROM_ADDRESS=<gmail account>` + the Gmail App Password at Vault `secret/vigil/notifications/email_password` (+ seed `VIGIL_DEMO_NOTIFY_EMAIL=<recipient>` so `coord.a` has an address), then drive a non-high→high crossing on `coord.a`'s site → one PII-free email arrives. **Gates:** ruff clean; check_specs **10**; full spine under Postgres **199 passed, 0 skips** (b2h +6); backend egress untouched beyond the new allow-listed SMTP host. The Phase-9 clinical-ops loop (9.1→9.6) is COMPLETE. Next: **9.7** synthetic trajectory demo (+ the live-send manual verification bundled with the LLM/Langfuse live checks).
  - [x] **Gate CLEAN-1 — remove committed key-probe + wire the assistant panel to the real backend (2026-06-18)** — repo hygiene + the last unwired frontend boundary; backend UNTOUCHED. **PART 1 (hygiene):** `git rm _keytest.py` (a throwaway live-OpenRouter key/connectivity probe committed by mistake at repo root — read its key from Vault/env at runtime, no secret value was in the file or needs scrubbing from history beyond removing the script) + added `_keytest*` to `.gitignore` so it can't be re-committed. **PART 2 (assistant wiring):** `components/vigil/assistant-panel.tsx` now imports the REAL async client from `@/lib/api` (`openConversation` → `POST /assistant/conversations`; `postMessage` → `POST /assistant/conversations/{id}/messages` 202; `pollJob` → `GET /assistant/jobs/{id}`) instead of the canned `@/lib/stubs`. The hardcoded fake answer ("Participant PT-1001 … 0.82 (high band)…") and the fabricated `sampleConversation` seed are GONE — the panel opens a conversation lazily, posts the question, polls the job to completion (capped → honest timeout), and renders the REAL turn. **Honest states:** empty start (neutral hint), in-flight (working… chip), the real answer, a guardrail **refusal** rendered as its own labelled bubble (`guardrail_decision === "blocked"`), and an honest **error** bubble (never a fabricated answer). **Types reconciled to the LIVE shape (no cast):** the polled `AssistantTurn` is exactly `{conversation_id, role, content, guardrail_decision, created_at}` — it carries **no citations field** (citations live in the observability `message_events.retrieved_chunks`, not the chat turn), so the panel renders content/refusal without inventing a citation UI; `types.ts` already matched. A live (non-stub) answer needs the real LLM key at run time (CI/`VIGIL_LLM_STUB` returns the deterministic stub turn — correct); the WIRING is what this gate delivers. `frontend/lib/stubs.ts` had no other importers and is DELETED. **Gates:** frontend `tsc --noEmit` clean (0 errors); `check_specs` 10; ruff clean; backend full spine under Postgres green (untouched). Next: **9.6** email notifier (Gmail SMTP/Vault/allow-list/CI-stub).
  - [x] **Gate 9.5 — per-user notification email + SCOPE-BOUND recipient resolution (2026-06-18) — THE SACRED Phase-9 routing gate** — no email is SENT (that is 9.6); this is the field, the settings path, the seed, and the resolution + its adversarial cross-site proof. **Field:** `user.notification_email` (nullable; migration `0010_user_notification_email` adds the column to the cross-tenant `user` table — its bespoke `user_scope` RLS UNCHANGED). **Settings:** new `/me` router — `GET`/`PUT /me/notification-email` set/clear the CALLER's OWN address (no user-id param → structurally only self; `EmailStr`-validated, clearable). Writes run under a new `user_self_session` (is_platform on the bespoke `user` table) constrained by the service to `id = scope.user_id` — uniform across roles (a CRO user has no home_sponsor, so a sponsor-bound write would fail the table's WITH CHECK). **Seed:** the demo coordinator `coord.a@vigil.example` is seeded with `notification_email` sourced from the **environment** (`VIGIL_DEMO_NOTIFY_EMAIL`) at seed time — NEVER a committed code constant (a test greps all of `vigil/**.py` and asserts the personal address appears nowhere); every other user defaults UNSET. **SACRED scope-bound resolution** (`notification_service.resolve_recipients(crossing)`): returns the notification emails of ONLY the users whose FULL resolved scope `scope.permits` the crossing's `(sponsor, trial, site)` tuple — the same SEC-1 primitive that guards participant reads; users without an email, and unresolvable scopes, are excluded; reads the cross-tenant user/grant tables under an is_platform session, narrowing via `scope.permits`, not the read. **Adversarial proof (mandatory, green under Postgres):** for a site-1 crossing, resolution INCLUDES the site-1 coordinator and EXCLUDES a site-2 coordinator (cross-SITE — the existence leak that must never happen) and EXCLUDES a Sponsor-B coordinator (cross-TENANT); a site-2 crossing resolves to site-2's coordinator, not site-1's; a crossing with no in-scope email → empty set, no error. **Gates:** ruff clean; check_specs **10**; full spine under Postgres **182 passed, 0 skips** (11 slow-deselected; +7 b2g incl. the sacred test); golden **5**; migration 0010 applies clean on the chain. No email sent. Next: **9.6** SMTP notifier (Gmail/Vault/egress/CI-stub).
  - [x] **Gate 9.4 — at-risk frontend surface (2026-06-18)** — surfaced the clinical-ops loop in the dashboard; backend UNTOUCHED (consumes 9.1/9.2/9.3 endpoints). **At-risk LIST** — new `frontend/app/at-risk/page.tsx` calling the scope-bound `GET /cohort?risk_band=high&sort=risk_desc` (9.2): coded-ids-only rows ranked by risk desc, each linking to the participant detail; role-gated (platform → "not available"); honest **loading / empty / 403-denied / error** states (never stale/fabricated rows). Added `/at-risk` to the nav + a `sort` param to `getCohort`. **Detail wiring** (`participant/[id]/page.tsx`) — the risk **trajectory** was already wired to the real `/risk/history` (RiskTrajectory, promotion-aware version boundaries); added the **9.3 recommended actions** section (operational suggestions; each "Log" button records the existing audited `/interventions` with the kind pre-filled — nothing auto-logged; viewing for all in-scope roles, logging gated to `CAN_LOG_INTERVENTIONS`); reasons (9.1 factors) already rendered via ContributingFactors. **Synthetic banner (hard contract):** non-dismissible, **flag-driven** — the detail banner renders from `detail.synthetic || risk.synthetic` (structural, not a toggle) so any synthetic-derived risk/trajectory/reason/action shows the disclosure; the at-risk list row carries a flag-driven `SYNTHETIC` badge from `CohortRow.synthetic`. **Types reconciled to the live endpoints** (no cast-fix): `RiskExplanation` extended with `recommended_actions: SuggestedAction[]` + `synthetic` (mirrors the 9.3 api.md shape); no shape mismatch found. **Gates:** frontend `tsc --noEmit` **clean (0 errors)**; backend full spine under Postgres **175 passed, 0 skips** (untouched); check_specs **10**; existing auth/cohort/participant/observability pages unaffected. Next: **9.5** per-user notification email + scope-bound routing.
  - [x] **Gate 9.3 — recommended protocol actions layer (2026-06-18)** — the "what to do about it" layer on the at-risk detail: OPERATIONAL coordinator next-steps, NOT clinical advice. New `vigil/services/recommended_actions.py` — a **static, transparent, deterministic** mapping (no LLM, no generated text) from risk band + the 9.1 attribution factors to a small ordered list of suggested actions, each mapped to the existing audited `InterventionKind` (call/visit_reschedule/reminder/note): missed-visit family (`consecutive_missed`/`cumulative_missed`/`missed`) → "Contact participant about missed visits" (call) + "Send a visit reminder" (reminder); `visit_index` → "Offer to reschedule the next visit" (visit_reschedule); a baseline "Flag for site/clinic follow-up" (note) for any high participant. **Closed catalog** (`APPROVED_ACTION_TEXTS`): a surfaced action MUST be one of four approved operational strings — nothing else can leak. Actions surface ONLY for the serious-risk (`high`) band; suggestions only — acting still goes through the audited `POST /interventions` (the `intervention_kind` pre-fills it; nothing auto-logged). Surfaced on `GET /participants/{id}/risk` (`RiskExplanation.recommended_actions` + `synthetic`) — the existing **scope-bound** detail (inherits SEC-1 + champion-only; no new read semantics). **Honesty:** these are operational next-steps, explicitly labelled "suggested coordinator actions", never medical/diagnosis/treatment; the synthetic flag governs the surface. **Tests (`tests/spine/test_b2f_recommended_actions.py`):** mapping deterministic (called twice → identical) + every action ∈ approved set + **no medical/diagnosis/treatment token** (regex guard) + maps to a real `InterventionKind`; missed-visit → contact (call); non-high band → no actions; no-recognized-factor → baseline only; end-to-end the risk detail surfaces operational actions + synthetic=True; **scope-bound** — a site coordinator gets 404 on an out-of-site participant's risk/actions (SEC-1 inherited). **Gates:** ruff clean; check_specs **10**; full spine under Postgres **175 passed, 0 skips** (11 slow-deselected); golden **5**; SEC-1 + wire3 regression green. Next: **9.4** at-risk frontend surface.
  - [x] **Gate 9.2 — serious-risk crossing detection + scope-bound at-risk surface (2026-06-18)** — **PART 1 (crossing detection, worker-side, idempotent):** new `risk_crossing` tenant table (migration `0009_risk_crossing`, FORCE RLS on `sponsor_id`, no platform bypass; in `TENANT_TABLES`). After the score writeback the worker compares each participant's PRIOR champion band (newest champion row before this run, via `champion_scores_by_participant`) to the new band; a **non-high → high** transition records ONE crossing (`scoring_repo.record_crossing`). **Idempotent by construction:** the crossing is anchored on `crossing_score_id` (UNIQUE — the champion score row that crossed), and only a genuine non-high→high transition records — so a re-score while still high, and a RETRIED job (whose prior band is now high), add NO new crossing; a high→non-high→high records a new one. The row carries the participant's full `(sponsor, trial, site)` scope + a `notified` flag for 9.5/9.6 to claim the email — **no email is sent here**. **PART 2 (scope-bound at-risk surface):** extended `/cohort` to honour `risk_band` + `sort=risk_desc` (previously declared in `CohortQuery` but IGNORED) — the at-risk list = `/cohort?risk_band=high&sort=risk_desc`. **Scope-bound (sacred, inherited):** the band filter/sort apply AFTER the existing RLS + SEC-1 `scope_filter.participant_visible` narrowing, so a site coordinator sees ONLY their site's at-risk rows; cross-tenant + cross-site both hold. Champion reasons (9.1 `top_factors`) + the `/risk/history` trajectory ref surface on the list/detail; coded-ids-only. **Tests (`tests/spine/test_b2e_crossing.py`, real Postgres, no torch via scorer-override):** crossing fires exactly once per transition (0.4→none, →0.7 one, stay-0.7 none, retry none, →0.2 none, →0.7 two); `record_crossing` idempotent on the same score row; crossing carries scope; **SACRED cross-tenant** — a Sponsor-A crossing is invisible to a Sponsor-B session (risk_crossing RLS); **SACRED at-risk scope** — site-1 coord's `?risk_band=high` excludes site-2's at-risk participant, cross-tenant denied, sponsor-oversight not over-narrowed, `sort` orders by risk, reasons surface, coded-only. **Gates:** ruff clean; check_specs **10**; full spine under Postgres **169 passed, 0 skips** (11 slow-deselected); slow LSTM suite green (b2b **7**, b2d **4**); golden **5**; SEC-1 + cohort regression green. Next: **9.3** recommended actions.
  - [x] **Gate 9.1 — REAL model-attribution reasons in the scoring writeback (2026-06-17)** — closed the empty-`top_factors=[]`/`reasons=[]` gap (`vigil/workers/tasks.py`) with GENUINE, leakage-safe per-participant attributions, honestly labelled — never invented clinical reasons (the 9.0 decision-2 contract). **Method = occlusion over each model's OWN inputs:** the **champion LSTM** re-scores with each sequence channel neutralised (set to 0 in standardized space = its train mean) and reports the signed `base − occluded` final-step delta per `SEQ_NUMERIC` channel (`LSTMScorer.attributions`, `method="lstm_occlusion"`); the **structural shadow** re-predicts with each ContractTransformer feature set to NaN (HGBR native-missing = neutral) and reports the delta (`StructuralScorer.attributions`, `method="structural_occlusion"`). Both perturb ONLY the model's already-guarded input features — no outcome/latent/provenance field is ever in the attribution surface; a **sacred leakage guard** runs `assert_no_outcome_features` on the reason feature names before writeback. Each reason carries the **method label + the synthetic flag** (`{"feature","contribution","method","synthetic"}`); `top_factors` mirror the top-k reason feature names. **Honesty:** a random/override scorer (`_demo_scorer`) writes NO reasons (it supports no attribution) rather than fabricating; participants with no observed trajectory get none. **Champion-only on the read surface (B4):** `risk_service`/`cohort_service` surface champion reasons; shadow reasons are stored, never surfaced. **Tests (`tests/spine/test_b2d_attribution.py`, slow/torch):** real attribution is non-empty + leakage-safe (features ⊆ `SEQ_NUMERIC`, `assert_no_outcome_features` passes, no forbidden token); attribution **tracks the inputs** (distinct trajectories → distinct attributions, proving not-fixed); `score_trial` writes real reasons with the synthetic label; champion reasons surface on the read path, shadow not. **Gates:** ruff clean; check_specs **10**; full spine under Postgres **160 passed, 0 skips** (11 slow-deselected = 7 prior + 4 new b2d); slow LSTM suite green (b2b **7**, b2d **4**); golden **5**; b2b refactor behavior-preserving. NOTE: the demo SEED still writes placeholder `top_factors` (`["missed_visits","hba1c_trend","bmi"]`) as bootstrap — overwritten by the first real LSTM score; emptying/replacing it is a small follow-up (demo/9.7). Next: **9.2** crossing-detection + scope-bound at-risk `/cohort`.
  - [x] **Gate 9.0 — Phase 9 spec reconcile + ratified decisions (2026-06-17)** — SPEC ONLY, no app code. Locked the clinical-ops contract into the specs. **Ratified:** (1) **serious-risk threshold = the existing HIGH band (>0.6)**, no new threshold; a crossing = champion-point transition non-high → high (`scoring.md § Phase 9`). (2) **reasons/`top_factors` = REAL model attributions** (SHAP/feature-importance for structural; actual sequence features for the LSTM), labeled synthetic on synthetic data; **hand-written/rule-invented clinical reasons FORBIDDEN**; 9.1 replaces the current empty-`[]` writeback (`scoring.md § Phase 9`). (3) **at-risk surface = EXTEND `/cohort`** (`?risk_band=high&sort=risk_desc`, currently ignored by the service), NOT a new endpoint; scope-bound via existing SEC-1 `scope_filter`, coded-ids-only (`dashboard.md § At-risk surface`, `api.md § cohort`). (4) **per-user notification email (Option A):** `user.notification_email` (nullable, defaults UNSET, set via `PUT /me/notification-email`); the demo coordinator `coord.a@vigil.example` is **seeded** with a recipient address sourced from the operator env (`VIGIL_DEMO_NOTIFY_EMAIL`, finalized in Gate 9.5 — never a committed literal) on that user record; **scope-bound recipient resolution is SACRED** — a crossing routes ONLY to users whose `Scope.permits` covers the participant's site (a mis-routed PII-free email leaks at-risk EXISTENCE), with an adversarial cross-site test (`domain.md § Notification routing`, `isolation.md § Phase 9`, `api.md § me`). (5) **email body = minimal PII-free doorbell** ("a participant at your site crossed the serious-risk threshold — log in: [deep link]") + synthetic label; no identity/clinical detail/factors; data lives behind auth (`isolation.md § Phase 9`). (6) **Gmail SMTP via stdlib smtplib, Arq job (never inline)**; SMTP host = new app-side allow-listed egress (deny-by-default, like the LLM/Langfuse hosts); App Password in Vault `secret/vigil/notifications/email_password` (+ env shim); recipients from user records (NOT a secret); **CI-stub `VIGIL_EMAIL_STUB` (no real send), live send = run-once manual check, not a CI gate** (`isolation.md § Phase 9`, `infra.md § Phase 9 notification egress`). (7) **crossing dedupe** — fires ONCE on the non-high → high transition, not on every rescore while still high; idempotent (`scoring.md § Phase 9`). (8) **crossing detection worker-side**, right after the writeback (the worker knows the prior champion band), recorded idempotently (`scoring.md § Phase 9`). (9) **synthetic label on EVERY Phase-9 output** (at-risk surface, reports, email) — the non-dismissible banner contract extends here (`scoring.md`/`dashboard.md`). Registered 5 new `##` headings in `check_specs.py` (scoring/dashboard/domain/isolation/infra); existing headings byte-identical; **check_specs PASS (10)**. No app code touched.

## Open TODO register
Synced from `TODO(...)` comments in the tree + decisions. (file:line · note)
- [x] extract.py:153 — RESOLVED 2026-06-05: `--live` run against hosted AACT, snapshot pinned (TODO removed)
- [x] clean.py:76 — RESOLVED: fabricated sample fixture retired; the only non-live substrate is now the REAL-AACT golden set (`tests/golden/`), so a sample can no longer be mistaken for real (TODO removed)
- [ ] targets.py:250 — revisit early_fraction once a real per-event timing source exists (still a labelled ASSUMPTION)
- [x] **EDA-before-cohort**: real extract → EDA → targets → synth (Phase 1 reorder) — COMPLETE; synthetic calibrated 40/40 PASS
- [x] **SPEC FIX (main):** CTGOV2 enum format ratified in `specs/data.md` (commit `857d505`); `ctgov_enums.py` normalization kept; `ECT` added as a `primary_purpose`. Code reconciliation committed (`980162d`).
- [x] **Withdrawal-reason vocab**: SET expanded (commit `2b4d554`) — added `STUDY_TERMINATED` (12.2%) + `ADMINISTRATIVE` (8.2%); censoring/ongoing now excluded from the mix (183,815 vol / 2,490 rows recorded separately, never counted as dropout). `OTHER` 50.7% → **17.54%** (honest floor; arm-level dropout unchanged, trial-mean 0.2018). `vocab.py`/`clean.py`/`report.py` reconciliation committed (`980162d`).
- [x] **Golden set replaces the fabricated fixture**: the deterministically-generated SAMPLE fixture (`ingestion/fixtures/`, `build_sample.py`) is **retired**. The ingestion clean-transform oracle is now `tests/golden/` — a frozen slice of REAL public AACT (snapshot 2026-06-05, 64 NCTs across every phase×sponsor_class×has_withdrawal×has_max_age stratum) with committed `raw/` + `expected/` CSVs + `selection.json` + `build_golden.py` + README. NO PHI, NO synthetic. `clean_snapshot(raw)` reproduces `expected/` via `assert_frame_equal` (`tests/test_golden_oracle.py`). The fast suite + the non-live pipeline run off it (`make golden`).
- [x] **Report-clobber guard (Gate 2 fix)**: a non-live run can no longer overwrite `data/reports` (defaults to `data/reports_fixture`, fails loud on an explicit `REPORT_ROOT` override — same pattern that protects `data/clean`). `data/reports/data_quality_report.json` regenerated from the REAL snapshot: ref_trial 73,073 / ref_arm 182,240 / 760 fail-loud drops / 2,490 censoring rows (183,815 vol).
- [x] **miss_probability leakage hole (spec/code drift) closed before B2b** (2026-06-10): `specs/scoring.md` § inv 4 claimed `assert_no_outcome_features` forbids `miss_probability` (the synthetic generator's latent hazard); `models/leakage_check.py::_FORBIDDEN_TOKENS` did not list it. Fixed: `miss_probability`, `time_to_event`, `event_observed`, `arm_real_dropout_rate` added to the shared forbidden set; three new tests in `tests/spine/test_engagement_leakage.py` assert the guard raises loudly. All 129 fast-suite tests green.
- [x] **CHEAP-FIX (Finding 2) — DONE (Gate B4)**: pan-indication test base rate / positive prevalence now recorded in `data/models/baselines/metrics.json` (`positive_prevalence` 0.4740 + `n_positives`/`n_total` on test/val) — the 0.697 PR-AUC reads honestly above its chance baseline from the artifact alone.
- [x] **Finding 3 (survival C-index sign convention) — VERIFIED CORRECT, not a bug**: `concordance_index(tte, -H, event)` in `models/t2d/survival.py:263` is correct; C-index 0.400 is a true negative result (anti-concordant on TTE ranking), not a double-negation artifact. No fix needed; flagged to prevent future re-review.

### Frontend wiring register (Phase 4/5) — endpoint → screen/boundary
Stub data layer is `frontend/lib/stubs.ts` (typed to `specs/api.md` via `frontend/lib/types.ts`). Each `// TODO(phase4/5)` below is unwired.
- [x] **Wire-1 (2026-06-11): `POST /auth/login` + `GET /auth/me` wired to the scoped layer** — login page calls real `/auth/login` (honest error state on bad creds, no silent pass), auth context (`lib/auth-context.tsx`) hydrates `me` from real `/auth/me` (role/scope from the VERIFIED token), logout revokes `jti`; fake auth removed (`STUB_ME` deleted from `stubs.ts`, login-page stub framing gone). Backend `MeOut` shape matches `types.ts` exactly — no drift. **Real-JWT role string ↔ frontend PLATFORM_ROLES gate verified end-to-end for the first time** via real `/auth/me` (`tests/spine/test_role_strings.py`): platform_admin `MeOut.role` is admitted by the parsed gate, coordinator is NOT; bad credentials → 401. `tsc --noEmit` clean. Refresh rotation left as `TODO(wire-later)` (stale token → /me clears it → re-login). Cohort/participant/monitoring/assistant stay stubbed (Wire-2/3).
- [x] **Wire-2 (2026-06-11): `GET /cohort` + `GET /cohort/summary` wired to the scoped layer** — dashboard (`app/page.tsx`) + triage (`app/triage/page.tsx`, already on `lib/api.ts`) render the REAL RLS-scoped cohort using the Wire-1 token; the frontend adds NO sponsor filter (RLS is the guarantee). Honest synthetic badge (`CohortRow.synthetic` from B4's champion-only data-driven flag) + real `top_factors`; honest empty + error/403 states (no stale/fabricated rows). Cohort stubs removed from `stubs.ts` (`getCohort`/`getCohortSummary` + `COHORT_ROWS`). **Cross-tenant invisibility verified on the real read path** (`tests/spine/test_wire2_cohort.py`): coordinator-A sees only Sponsor A rows, Sponsor B cannot see A's participant; synthetic honesty both directions through the cohort response; CohortRow shape matches `types.ts`. `tsc --noEmit` clean. **No-endpoint UI fields (do NOT fabricate):** `riskTrend` (sparkline) + `daysEnrolled` have no `CohortRow` field — rendered as empty/`0` placeholders pending a risk-history endpoint / `ParticipantDetail.enrolled_at` (Wire-3). Participant/interventions/monitoring stay unwired (Wire-3 / later).
- [x] **Wire-3 (2026-06-11): participant detail + `/risk` + `/risk/history` (sparkline) + interventions wired to the scoped layer — LAST frontend wiring gate.** `app/participant/[id]/page.tsx` now renders the REAL champion score (`/risk`, B2c champion-only — shadow never surfaces), the REAL cross-version **risk trajectory** (`/risk/history`, H2b), **real `daysEnrolled`** from `ParticipantDetail.enrolled_at` (closes the Wire-2 placeholder), and an audited **intervention round-trip** (POST→GET, actor = authenticated user server-side). New `RiskTrajectory` component makes the **promotion boundary VISIBLE** — per-version segment breaks + a dashed boundary line + `→ <model_version>` label at each change (no smoothed single line); synthetic points drawn distinctly (hollow ring) from real (filled); empty → honest empty state. `types.ts` +`RiskHistoryPoint`/`RiskHistory`; `api.ts` +`getParticipantRiskHistory`; participant + intervention stubs removed from `stubs.ts`. **Backend deviation (forced):** the participant-detail + interventions ROUTER endpoints were Phase-5 stubs (returned `trl_stub`/`iv_stub`/fabricated `enrolled_at`), so wiring the frontend to them would surface fake data — wired them to the EXISTING real repos instead (`participant_service.py`: scoped detail with champion-sourced `synthetic` + site-role-only identity; real audited `log_intervention`/`list_interventions`; `tenancy.list_interventions` added). `/risk`+`/risk/history` were already real (B2c/H2b). 4 spine tests (`tests/spine/test_wire3_participant.py`: detail-is-real, champion-only on `/risk`+`/risk/history` with shadow present, cross-tenant 404 on all 5 participant endpoints, intervention round-trip + audit). `tsc --noEmit` clean. **Phase 4 done-when MET: coordinator login → scoped cohort → open participant → real champion score + cross-version trajectory + audited intervention.** Residuals: eslint deferred, `/auth/refresh` rotation `TODO(wire-later)`, monitoring/cost + assistant screens still stubbed (later gates).
- [x] **Honesty-of-record reconciled (2026-06-11)**: doc/card/docstring only — no logic change. (1) scoring idempotency self-contradiction fixed → `specs/scoring.md § Execution model` + `vigil/workers/tasks.py` docstring/step-1 comment now state **append-truth** (writeback APPENDS a timestamped history row; an Arq retry APPENDS a new point and is NOT upsert-idempotent — cosmetic dupe, never a cross-tenant/non-champion leak). (2) `data/models/PHASE3_CARD.md` per-indication table now surfaces real **`n_test_arms`** (T2D 516, ALZ 172, MDD 152, …, from `indication_pr_auc.json`) beside each test-fold PR-AUC instead of total `n_arms`; ALZ 0.7747 re-labeled **DIRECTIONAL** (172 test arms, single split, no CI — may overlap PSO). No fabricated CIs (deferred hardening track). Remaining consolidation-audit items tracked, **benign-until-deferred-feature**: multi-breach last-known-good can target an already-bad version (only reachable once drift-signal delivery is wired); H2b champion-interval assumes regime-distinct version strings (seam with regime-not-threaded; convention-only today); `/auth/refresh` 401-UX gap (stale token → stuck loading vs clean re-login). check_specs 10.
- [x] **`GET /monitoring/models` + `/drift` → Monitoring — DONE (Gate 6.4)**: `app/monitoring/page.tsx` wired to real `getModels` (champion/shadow from routing_state) + `getDrift` (honest-empty state); monitoring stubs removed from `lib/stubs.ts`.
- [x] **`GET /monitoring/cost` (+`/models`) → Cost & Routing — DONE (Gate 6.4)**: `app/costs/page.tsx` wired to real `getCost` rollups (honest-zero note); `CostReport`/`CostRollup` added to api.md (Gate 6.2) + types.ts.
- [x] **`POST /assistant/conversations` + `/messages` (202) + poll `GET /assistant/jobs/{id}` → docked assistant (Gate CLEAN-1, 2026-06-18)** — wired to the real `@/lib/api` async client; hardcoded fake answer + `sampleConversation` removed; honest loading/answer/refusal/error states; `lib/stubs.ts` deleted (no importers remained).
- [ ] No-endpoint gaps to flag at wire time: per-participant risk history (sparkline/trend), `daysEnrolled` in cohort rows, assistant-generated prose for the explanation card — none have an api.md field (`app/page.tsx:27,29`, `app/triage/page.tsx:31,33`, `app/participant/[id]/page.tsx:59`)

### Process / housekeeping TODOs
- [ ] **Enforce the evaluation contract in `check_specs.py`** — extend the conformance check as each phase lands: a **models** phase must have a held-out split; a **RAG** phase must have an eval set; a **transform** must have a golden set (per `specs/data.md` "Evaluation contract").
- [x] **Frontend typecheck in CI** (2026-06-11): `tsc --noEmit` now runs in CI (`ci.yml`: Node 22 + `npm ci` from new committed `frontend/package-lock.json` + `npx tsc --noEmit`). The `ignoreBuildErrors: true` no-op is CLOSED — removed from `next.config.mjs`, so `next build` also validates types (confirmed: build runs TypeScript and passes). **2 pre-existing type errors fixed** (both TS2307 in `components/vigil/index.ts`: dead re-exports of deleted `./participant-table` + `./header` — removed; no importers, no runtime change). tsc now **0 errors**. eslint **DEFERRED**: the project has no eslint config/install post-Next-16 (`next lint` removed); a fresh flat config is out of scope here — re-add when frontend wiring lands. This establishes the green typechecked baseline for the upcoming auth/cohort/participant wiring gates. Backend untouched (ruff/check_specs 10/golden/full spine 90 still green).
- [ ] **Group tests by phase** — reorganize into `tests/ingestion`, `tests/spine`, `tests/models` (markers/paths) so phase suites run independently.
- [ ] **Remove unused frontend files if any remain** — currently `frontend/components/vigil/header.tsx` and `participant-table.tsx` are superseded by `AppNav` / `TriageTable` and unused; delete or repurpose.

ROADMAP TODO (RAG stage): build the public "receptionist" RAG agent over real analyzed studies — name, sponsor/industry (class + name), results/findings, study_url link, searchable by condition/phase/sponsor. Behaviour: on broad query (e.g. "oncology studies") either return a list or ask 1–3 narrowing questions (phase, sponsor type, status). Requires re-extract of AACT text + sponsor name into a ref_corpus (NOT modelling features, NOT tenant data). Isolated from app DB. Spec in data.md + rag.md at that stage.

ROADMAP TODO (Phase 6 / demo): live demo script that generates fresh synthetic participants on the fly and feeds them through ingest→score→rank, shown updating on the dashboard. Two modes: (1) in-distribution → demonstrates live scoring; (2) --drift → generator parameters shifted so PSI/KS cross threshold, drift fires on the monitoring screen and triggers audited fallback (Phase 4 routing). Builds on 5a-(i) participants-as-synthetic-members-with-series. Frame to audience as simulated incoming ePRO batches (synthetic, no PHI).
## Decisions log (settled)
- Sponsor = hard tenant boundary (RLS). CRO scoped per assignment.
- ClinicalTrials.gov/AACT = build-time only; synthetic cohort proves method validity, not clinical prediction.
- pgvector (not Pinecone); hand-rolled router/agent orchestration (NOT LangGraph, NOT the MCP protocol — neither is a dependency or import); Langfuse tracing.
- Two surfaces; public Guide isolated in three layers.
- uv toolchain; release agent prepares commits; CI runs the same checks.
- **Evaluation contract ratified** (`specs/data.md` + CLAUDE.md per-phase ritual): one test artifact per purpose — **golden = transforms, eval set = RAG, held-out split = models**. Choosing the wrong type is a spec violation. `check_specs.py` enforcement tracked in housekeeping TODOs.

