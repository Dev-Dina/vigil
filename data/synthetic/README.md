# Synthetic cohort

SYNTHETIC — calibrated to aggregate AACT statistics, NOT real participants, method-validity only, no PHI.

Seed: 20240601 (bit-identical regeneration). Every row has `synthetic = True`.
