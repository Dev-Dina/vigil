[project]
name = "vigil"
version = "0.0.0"
description = "Clinical-trial retention intelligence platform"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff",
    "pytest",
]

[tool.uv]
package = false

[tool.ruff]
target-version = "py312"
